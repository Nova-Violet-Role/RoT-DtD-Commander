<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T06:46:52Z-pid667 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (companion-audit.sh whole, companion-audit.dtd whole, checker-controls.sh 100-145, spdx-sweep.sh whole, plus counts-sweep, controls-sweep, contract-audit and readme-index run), `REUSE.toml`, `.gitignore` (diff and `check-ignore -v`), `README.md` (badges 22-27, index 199-225, claims rows 588-613, verify block 637-652), `CHANGELOG.md` (111-190, 459-527), `.github/workflows/gate.yml` (steps 64-153), `dtd/` and `dtd/sigil/` (slop sweep, both modes), `lib/` (ai-slop, ordinals, ceiling), `artifacts/research/` (the tracked record, and the directory walked on disk), and the whole 669-file changed set scanned byte-wise for CR, BOM and control bytes.

Instruments run: `lib/ai-slop.mjs controls` prints `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8` with every landed proof shown; `lib/ordinals.mjs controls` prints `ordinals controls: ok (0 failing)`. `contract-audit.mjs`: 1928 declarations, 0 unused, 0 law gaps. `counts-sweep.mjs`: 139 commands, 22 skills, 5 agents, checked 166, guards 31, checker controls 24, 61 places in step; `--controls` 7 run, 0 failing. `readme-index.mjs --check`: README block in step. `controls-sweep.mjs`: 33 claims, 0 drifted. Encoding: 669 of 669 changed files scanned, 0 CR, 0 BOM, 0 control bytes. SPDX coverage re-derived in node over `git ls-files`: 843 tagged, 17 covered by REUSE.toml, 0 missing, 0 tags past line 25.

### 🩺 Findings

<finding file=".gitignore" line="52" severity="high" confidence="measured">artifacts/research/.rot-moe/ holds 18 files inside the work tree: fifteen rot-route-*.jsonl session transcripts of 149,811 to 361,167 bytes, animus-distillate.md at 53,940 bytes, and a 2-byte .gitignore whose whole content is the single line `*`. git check-ignore -v artifacts/research/.rot-moe/animus-distillate.md names that foreign file, `artifacts/research/.rot-moe/.gitignore:1:*`, as the only rule reaching it; the repository .gitignore names rot-moe nowhere, although this phase added rules at lines 40 to 52 for CLAUDE.md, .claude/, artifacts/cache/, artifacts/sigil/ and artifacts/research/companion-*.json. Failure: the plugin rewrites or drops its own .gitignore, or a checkout omits it, and the next `git add -A` stages roughly four megabytes of raw prompt and response transcripts into a public AGPL repository. git status --porcelain prints clean today and cannot warn, and checker/spdx-sweep.sh enumerates with --exclude-standard so it never sees the directory either.</finding>

<finding file="checker/companion-audit.sh" line="137" severity="medium" confidence="measured">The comment reads "cwd is the scratchpad, not the repo: a nested session's hooks must not touch the tree (measured: CRLF .gitignore, .claude/, .codemap/, CLAUDE.md)". The invocation the changelog publishes at CHANGELOG.md:113 passes artifacts/research as the out-dir, which is a directory of the repo, and the nested sessions wrote .claude/ (two files), .rot-moe/ (18 files) and CLAUDE.md there. Three of the four artifacts the comment names as measured hazards are present in the tree right now. The mitigation stated is a claim about cwd that the shipped command line does not satisfy.</finding>

<finding file="checker/companion-audit.dtd" line="57" severity="medium" confidence="measured">The block at lines 47 to 58 is the map of which instrument holds which law, and it stops at "5: the runner's exit 124 branch". LAW.COMPANION.7 and LAW.COMPANION.8, added in this phase at lines 44 and 45, appear nowhere in it, and neither do the controls that hold them, M20 and M21. A reader auditing the contract by that map concludes two of the eight laws govern nothing, which is the exact defect the map exists to make visible.</finding>

<finding file="CHANGELOG.md" line="185" severity="medium" confidence="measured">The row claims `node lib/ai-slop.mjs sweep dtd/sigil artifacts/research --max 0`: 22 files, 0 slop. The gate step at .github/workflows/gate.yml:149 runs the --tracked form of the same sweep, and it prints `tracked 21 of 22 files` then `21 files, 0 slop`. The twenty-second file is artifacts/research/CLAUDE.md, untracked and ignored by .gitignore:41. The published number counts a plugin-written file the repository does not own and the gate cannot reach, so no step re-runs the row.</finding>

<finding file="artifacts/research/companion-9.0.0.md" line="3" severity="medium" confidence="measured">Lines 1 to 3 of the tracked record are the SPDX line, the copyright line, then a blank. There is no `<!-- companion run: ... -->` line, although checker/companion-audit.sh:159 writes a three-line header carrying one. Commit d13c383 is titled "the record stamped by its run" and its diffstat shows it changed this same file by 26 lines. The record the repository publishes as the phase 9.0.0 audit therefore carries no stamp, and the commit's headline claim is not true of the file that commit edited.</finding>

<finding file="checker/companion-audit.sh" line="64" severity="low" confidence="measured">The LAW.COMPANION.7 arm is wrapped in `if [ -n "$stamp" ]`, and --score defaults the stamp to empty at line 86. checker-controls.sh:83 defines its score() helper without a stamp argument, so M12, the planted pass that must score as a pass, is measured with LAW.COMPANION.7 switched off. Only M20 passes a stamp. The law is unenforced on every hand re-score of a record, which is the one path where a stale record is plausible.</finding>

<finding file="checker/companion-audit.sh" line="71" severity="low" confidence="reasoned">The LAW.COMPANION.8 check builds `heads` with `awk '/^### / && NF greater-or-equal 3 { print $3 }'` over the whole record, then demands the exact string "Scope Findings Verdict Next ". Any third-level heading the companion quotes from a file it audits, and this repository's README carries several, adds a fifth word and refuses a sound pass at line 74. The gate reads red on a correct answer, and the message names LAW.COMPANION.8, which points the reader at the grammar rather than at the quotation.</finding>

<finding file="checker/spdx-sweep.sh" line="31" severity="low" confidence="reasoned">covered() matches with `case "$f" in $p|*/$p`, and a shell case glob `*` matches a slash, so the annotation path `checker/*.json` also covers checker/any/depth/x.json. REUSE.toml:19 states the opposite intent in its own comment: "Named by directory, never **/*.json: a JSON that is not ours, vendored or generated, is reported uncovered rather than relicensed in silence". A vendored JSON dropped one level below any named directory is relicensed in silence, which is what the comment says must not happen.</finding>

### 🩺 Verdict

The instruments of this phase are sound where they were measured. Every control ran and every landed proof printed: the ai-slop suite shows its sloppy fixture at 38 hits and its clean fixture at 0, the rotation self-overlap at 1 against a bound of 0.5, and the boundary case holding at equality. The contract audit closes at 1928 declarations with no unused declaration and no law gap. The 669 changed files carry no CR, no BOM and no stray control byte. The SPDX position the phase claims is real: re-derived independently in node over git ls-files, 843 files tagged, 17 covered by REUSE.toml, none missing, and no tag hiding past the twenty-fifth line. Counts agree in 61 places, the README index agrees with the resolved tree, and the two new laws are numbered densely at 7 and 8 with controls M20 and M21 that both trip.

What fails is the boundary between the audit and the repository it audits. The runner asserts that its nested session does not touch the tree, and the tree contains four megabytes of that session's raw transcripts, held out of the index by a two-byte file the plugin wrote and the repository neither tracks nor names. Four sibling rules were added to .gitignore in this same phase for smaller leaks; the largest one was left to a foreign file. The same seam produced the other findings: a record the stamping commit did not stamp, a changelog number the gate step cannot reproduce because the two forms disagree about which files the repository owns, and a contract map that stopped being updated two laws ago. None of these are failures of the instruments. They are the tree drifting from what the instruments and the commit messages say about it.

### 🩺 Next

Add `.rot-moe/` to the repository .gitignore beside the rules at lines 40 to 52, and delete artifacts/research/.rot-moe so the concealment stops depending on a file the tooling owns. Extend the spdx sweep or the encoding sweep with an arm that walks artifacts/ on disk and reports any directory carrying its own .gitignore, so the next tool that hides itself is named rather than trusted. Add laws 7 and 8 with M20 and M21 to the instrument map at checker/companion-audit.dtd:57. Re-run the companion so the record is written with the stamp its law requires, and make CHANGELOG.md:185 quote the --tracked form the gate runs so the number and the step are the same measurement. Make the stamp argument mandatory in --score, or state in the usage line at :8 that omitting it disables LAW.COMPANION.7.

COMPANION VERDICT: fail
