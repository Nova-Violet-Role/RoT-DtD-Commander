<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T08:03:32Z-pid1209 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (companion-run.sh, companion-audit.sh, companion-audit.dtd, checker-controls.sh, counts-sweep.mjs, controls-sweep.mjs, contract-audit.mjs, gate-sync.mjs, enum-sweep.mjs, plates.mjs, badges.mjs, subsets-sweep.mjs, readme-index.mjs), `lib/` (typography.mjs, ai-slop.mjs, ordinals.mjs, ceiling.mjs, arm.mjs), `dtd/` (typography.dtd, cc-figure.dtd) and `dtd/sigil/`, `src/commands/` (typography-dtd.md), `src/skills/dtd-core-dtd/references/` and its built twin under `skills/`, `.github/workflows/gate.yml`, and the root docs `CHANGELOG.md`, `README.md`, `package.json`.

Ran under the wrapper: `lib/ai-slop.mjs controls` (0 failing, 117 banned phrases, 8 measures, 8 laws), `lib/ordinals.mjs controls` (0 failing), `lib/typography.mjs controls` (34 run, 0 failing — the CHANGELOG line re-runs), `lib/ai-slop.mjs sweep dtd/sigil --max 0` (5 files, 0 slop — re-runs), `lib/ai-slop.mjs src/skills/dtd-core-dtd/references/subsets.md` (alive, every measure holds), `git grep -P` sweeps for CR and U+FEFF, `git log v8.0.0..HEAD`. Both grep sweeps were controlled against a pattern proven to return non-zero on the same paths before their zero was read: CR is zero across `src dtd checker lib commands agents skills`. `src/skills/dtd-core-dtd/references/subsets.md` and `skills/dtd-core-dtd/references/subsets.md` are byte-identical.

Disclosure under LAW.COMPANION.2: before switching to the wrapper I ran read-only shell forms outside it (`cat`, `ls`, `sed -n`, `grep`, `head`, `od`, `diff`) with no ceiling and no closed stdin. Nothing was written, moved or committed; the runner's `tree_state` comparison is the check on that claim, not my word.

I did not run `checker/counts-sweep.mjs`, `checker/contract-audit.mjs` or `checker/controls-sweep.mjs`, though the wrapper admits all three, because reading them showed each writes into the tree — see findings 2 and 3.

### 🩺 Findings

<finding file="checker/companion-run.sh" line="71" severity="high" confidence="measured">The reading-verb set admits the dashless tokens `check`, `sweep`, `list`, `table`, `measure`, `score` and `load`, and four engines treat any argument that is not `--check` or `--controls` as their write mode: `bash checker/companion-run.sh node checker/readme-index.mjs check` reaches readme-index.mjs:392-393 and overwrites README.md, docs/families-map.svg and docs/families-map-dark.svg. Landed proof: `bash checker/companion-run.sh node lib/ceiling.mjs check` printed zero `companion-run: refused` lines and passed the token through — the exit 2 came from ceiling.mjs's own usage line, not the wrapper. plates.mjs:277, badges.mjs:220 and subsets-sweep.mjs:131 fall through the same way. The wrapper special-cases `build` to demand `--check` at line 73, so the dashed/dashless distinction was known when `check` was left in the list beside `--check`.</finding>

<finding file="checker/counts-sweep.mjs" line="52" severity="high" confidence="measured">companion-run.sh:61 admits `counts-sweep.mjs` bare, and a bare run calls `main()` (line 281) then `measure()`, which at line 52 runs `bash checker/checker-controls.sh` with cwd at the repository root. That script writes `zz-tree-control.tmp` into the root (checker-controls.sh:161) and spawns a nested `claude` session through companion-audit.sh (checker-controls.sh:164-167). One admitted command therefore both writes into the tracked tree and spawns an agent — the two acts LAW.COMPANION.1 names first, granted by the instrument built to refuse them.</finding>

<finding file="checker/contract-audit.mjs" line="105" severity="high" confidence="measured">companion-run.sh:61 admits `contract-audit.mjs` bare, and a bare run writes `dtd/zz-control.dtd` (line 105) and `dtd/zz-order.dtd` (line 116) into the tracked tree, removing them in a `finally`. The wrapper's ceiling is 60 s (companion-run.sh:79); a SIGKILL at the ceiling skips the `finally` and leaves a planted .dtd in `dtd/`, which the runner then reads as the companion's own breach at companion-audit.sh:171. The file's comment at line 127 states the temp-tree reasoning — "so a fired ceiling leaves nothing behind" — for plant3 and plant4 only; the two plants above it still write into `dtd/`.</finding>

<finding file="checker/checker-controls.sh" line="181" severity="medium" confidence="measured">M28 asserts the wrapper admits an engine "by what it does" but exercises only three arms: a writer refused by name (`spdx-add.mjs`), a bare run refused (`plates.mjs`), and `build` without `--check`. No control plants a dashless reading verb against a writing engine, and no control plants a bare run of a name that IS on the line 61 list. Both holes in findings 1 to 3 sit exactly in the untested arms, which is why they survived the nineteenth pass green.</finding>

<finding file="dtd/typography.dtd" line="145" severity="medium" confidence="measured">`glyph/@class` enumerates all six classes unconditionally while `typo.block.module` is IGNORE (line 111), and `typeset` keeps `glyph*` in its content model in every driver (line 199). A typeset carrying `<glyph class="block" .../>` in that slot is therefore valid against the default subset, contradicting LAW.TYPO.4 (line 217) and CHANGELOG.md:47-48, "a switched-off class is invalid against the subset". lib/typography.mjs:364-371 tests only the class *elements* and the content model, so no control can trip on it, while the enumeration control at typography.mjs demands a producer for class(6) — the tree is required to produce a class the default grammar has no element for.</finding>

<finding file="checker/companion-audit.sh" line="169" severity="medium" confidence="measured">The exit-124 branch returns before `tree_after` is taken at line 170, so a companion that writes into the tree and then runs past the wall-clock ceiling has its write neither detected nor named: the run is UNAUDITED for the ceiling, and the leftover file is absorbed as `tree_before` on the next run (line 123), where the comparison then comes out equal. Combined with finding 3, a ceiling fired inside `contract-audit.mjs` plants a .dtd that no later audit reports.</finding>

<finding file="checker/checker-controls.sh" line="161" severity="low" confidence="measured">M25 plants `zz-tree-control.tmp` in the repository root rather than a temp directory, with the `rm -f` on the same line and no trap. A kill between the write and the removal leaves the file in the root, and every later `--tree-state` reads it as part of the baseline.</finding>

<finding file="lib/arm.mjs" line="58" severity="low" confidence="measured">The BOM stripper is written with a raw U+FEFF inside the regex literal, `.replace(/^\ufeff/, '')` spelled with the character itself rather than the escape. `git grep -P '\x{FEFF}'` returns this line as the tree's only hit; the file itself carries no BOM (first bytes are `2f 2f 20 53 50 44 58`). Any editor or filter that drops zero-width characters silently reduces the pattern to `/^/`, which matches an empty string at position 0 and strips nothing — a stripper that fails open with no diff a reviewer can see.</finding>

### 🩺 Verdict

The phase's declared work measures true where it can be measured: typography controls re-run at 34 run, 0 failing, the `dtd/sigil` slop sweep re-runs at 5 files, 0 slop, the quoted subset and its built copy are byte-identical, the checker-controls span and the two README claims agree at thirty-one controls M0 to M28, LAW.TYPO.1 to 8 and LAW.COMPANION.1 to 8 are in sequence, and no CR byte exists in the seven folders swept.

The wrapper introduced by the last two commits does not hold. `checker/companion-run.sh` exists to make LAW.COMPANION.1 and 2 mechanical — the companion never writes, never spawns — and its own head comment says an engine is now "admitted by what it does". Three ways through it were measured this pass: a dashless `check` reaches four engines' write mode and rewrites README.md and two svg plates; `counts-sweep.mjs` bare writes into the repository root and spawns a nested `claude`; `contract-audit.mjs` bare writes two .dtd files into `dtd/` and loses its cleanup to the 60 s ceiling. The commit message of 169fe07 claims the property the code does not have, and M28 is the control that would have caught it if it had planted the arms it skipped. A guard whose refusals were never tripped in the shape that matters is the defect this repository's own laws are written against.

### 🩺 Next

Cut `check`, `sweep`, `list`, `table`, `measure`, `score` and `load` from the dashless set at companion-run.sh:71, keeping only their `--` spellings, and give `readme-index.mjs`, `plates.mjs`, `badges.mjs` and `subsets-sweep.mjs` an explicit refusal by name the way `spdx-add.mjs` has one at line 56. Drop `counts-sweep.mjs` and `contract-audit.mjs` from the bare-run list at line 61 until their plants move to `mkdtempSync` and their child spawns are gated; `contract-audit.mjs` lines 104-117 need the same temp-tree treatment its own line 127 comment already applies to plant3 and plant4. Add M29, a dashless reading verb against a writing engine, and M30, a bare run of every name on the line 61 list asserting the tree is unmoved after each. Move the `tree_after` comparison above the exit-124 branch in companion-audit.sh so a fired ceiling still reports a dirty tree. Either gate `glyph/@class` on the module entities in dtd/typography.dtd or restate LAW.TYPO.4 to cover only the class elements, and add the control that plants a block glyph under the default driver.

COMPANION VERDICT: fail
