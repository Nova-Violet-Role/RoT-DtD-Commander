<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read this pass, naming the operator's focus first: `.claude-plugin` (both manifests, whole), `skills` and `src/skills` (every SKILL.md front matter, plus `dtd-core-dtd/references/subsets.md`), `CITATION.cff` (whole), `checker` (hosted-plugin.json whole; companion-audit.sh, pack-claude-ai.mjs, counts-sweep.mjs, release-notes.mjs, seal-secret.mjs, subsets-sweep.mjs read in part), `.rot-lists` (all seven declarations, whole), `LICENSES` (three files, sizes and SPDX census only), `agents` (five files, DOCTYPE and rule span), `bin` (adiutor.mjs C31 and the RUNTIME list of rot-dtd-commander.mjs), `monitors` (manual.json whole). Also `dtd` (cc-sigil, cc-cache, cc-ask, the census of 31 subsets), `lib` (list, cache, sigil, regression, ai-slop, figure paths), `.github/workflows/gate.yml` whole, `.gitignore` whole, `CHANGELOG.md` 9.0.0 section, and the root manifests.

Ran: `lib/ai-slop.mjs controls` (0 failing, ban list 117, 8 measures, 8 laws), `lib/ordinals.mjs controls` (0 failing), `bin/adiutor.mjs controls` (31 run, 0 failing), `contract-audit` (1928 declarations, 0 unused, 0 law gaps), `counts-sweep` (57 places in step), `controls-sweep --check` (30 claims, 0 drifted), `enum-sweep` (5619 enumerations, 0 findings), `subsets-sweep --check` (28 blocks), `pack-claude-ai --controls` (27 run, 0 failing), `gate-sync` (79 commands, 0 missing), `release-notes --versions` and `--controls`, `list reach --repo-only` / `table --check` / `controls`, `starlist controls`, `about-sweep`, `badges --check`, `engines-sweep`, `heading-sweep`, `frontmatter-sweep`, `regression claims|retenue|unreached`, and the figure, geometry, typography, chain, cache, form, record, schematic, encoding and cross-os controls. A byte sweep of all 866 tracked files found no CR, no BOM and no stray control byte. `ai-slop` on the twelve prose files that changed reports `alive=yes` for every one.

One breach of LAW.COMPANION.1 to disclose: I invoked `checker/glossary.mjs`, `checker/plates.mjs` and `checker/readme-index.mjs` without `--check`, and those three verbs write their generated output. The renders were byte-identical, so `git status --porcelain` afterwards showed only `artifacts/research/companion-9.0.0.json`, which was already modified when this pass opened. Nothing else in the tree moved.

### 🩺 Findings

<finding file=".gitignore" line="30" severity="high" confidence="measured">The cache this release introduces writes session content into the working tree with no ignore rule over it. `lib/cache.mjs:226` resolves `cachePath(command, root = process.cwd())` to `<cwd>/artifacts/cache/<command>.nt` and `lib/cache.mjs:354` writes it; `dtd/cc-cache.dtd:59-63` fixes the fields it carries as the task the run restated, every answer taken with its header, the gate state and the step the run was about to take. `git check-ignore -v artifacts/cache/ask-me-questions-dtd.nt` exits 1 and names no rule; the same probe over `artifacts/sigil/run.json` (written by `lib/sigil.mjs:252-261` under SIGIL.dir) is also unmatched, while `dist/a.zip` matches `.gitignore:30`. `artifacts/` is a tracked directory here, holding `artifacts/research` and `artifacts/amplify-codebase`, so a save made while developing in this checkout appears as untracked content beside files the operator commits from, and `git add -A` sweeps it into a public AGPL repository. The comment on `.gitignore:29` records that exact hazard being paid for once already, for `dist/`, in the fifth companion pass. Two new writers landed in 9.0.0 and neither got the line.</finding>

<finding file="checker/hosted-plugin.json" line="31" severity="medium" confidence="measured">The recorded expectation for the next upload contradicts the fix it describes in the same sentence. `surfaceReported.why` reads "the archive renames those skills, so the 9.0.0 upload should report 137 (139 minus the two collisions)". The rename is what removes the shadowing: `pack-claude-ai.mjs:232-235` maps `ai-slop-dtd` to `ai-slop-gate-dtd` and `setup-ralph-dtd` to `setup-ralph-loop-dtd`, `renameSkillPath` and `renameSkillName` apply it to the archive and its front matter, and control C18 reports "2 of 2 renamed, 0 stale, frontmatter name agrees: true". With no name shared, the surface should report 139 commands. 137 is the number the rename failing would produce. H12 compares `components`, never `surfaceReported`, so nothing in the gate can catch the arithmetic, and the reading that means the archive is correct would be filed as a divergence while the reading that means the rename died would be filed as confirmation.</finding>

<finding file="checker/release-notes.mjs" line="216" severity="low" confidence="measured">`--versions` compares six sources and prints five. Line 122 walks `['plugin.json', 'marketplace.json metadata', 'marketplace.json plugin', 'CITATION.cff', 'CHANGELOG.md top section']`, and the control at line 156 proves a stale citation is named. The display loop at line 216 drops `CITATION.cff` from its list, so the run I made prints package.json, both marketplace fields, plugin.json and the changelog and stops. The guard holds; its output hides the one row the tenth pass found sitting five majors behind, which is the row a reader inspects a release by eye to check.</finding>

<finding file=".rot-lists/file-white.dtd" line="23" severity="low" confidence="measured">`LIST.entry.figure` was added this release and calls itself "one figure element on disk, the source both renderers of lib/figure.mjs read", but the tree holds no such file and nothing writes one. A census of every tracked path by extension returns md, mjs, svg, dtd, sh, json, yml, gif, tape, txt, jsonl, py, ts, cff and xhtml, with no `figure`; `git grep -F ".figure"` over lib, dtd, checker, src and bin returns only property accesses such as `s.figure.xml`, and `lib/geometry.mjs:292-294` and `:529` write `.json`, `.svg` and `.cells.txt`. The third clause of LAW.LIST.4 refuses an extension present-and-unlisted, so `reach` cannot report a listed class with no producer, and the entry stands with nothing behind its description.</finding>

<finding file="lib/ai-slop.mjs" line="633" severity="low" confidence="measured">`sweep` given a file dies on a Node stack instead of refusing by name. Line 633 calls `walk(presolve(d), files)` over each argument before any check that the argument is a directory; `node lib/ai-slop.mjs sweep README.md` from the repository root prints an uncaught `ENOTDIR: scandir` trace and exits 1. The refusal three lines below at 635-638 is the one this module wrote for the case, worded "a sweep of nothing is refused" and built so a renamed directory goes red rather than green, and a file argument never reaches it. Every gate step at gate.yml:147-151 passes real directories today, so the crash is confined to a mistyped argument, and a tree whose refusals otherwise name the edit answers this one with a stack.</finding>

### 🩺 Verdict

The declared machinery holds. Every count in the three manifests and CITATION.cff matches what `counts-sweep` measures at 57 places, the C-rule span reads C16 in the manifest, the checker and all five agents, contract-audit finds 1928 declarations with no gap in any law sequence, the sigil tiers sum to the 125 the subset declares with no form in two tiers, the eight `LAW.CACHE` laws are dense from one and control C31 trips on each branch of the gate, and `cc-cache.dtd`'s claim that `ASK.exhausted` offers start and save is true at `cc-ask.dtd:129`. The CHANGELOG's own arithmetic survives an independent count: 119 sources include `cc-cache.dtd` through a resolved parameter entity, which is the number it claims. No CR, no BOM, no control byte anywhere in 866 files. The `seal-secret` controls that the gate never runs carry the `rer-outside: manual` marker the RER law requires, with the reason written out, and its blake2b is pinned to the RFC 7693 vector for the empty string.

What fails is newer than the guards. The save choice arrived with a file path, a fixed form and eight laws, and without the one line that keeps the file out of a commit. The same release added a second writer under `artifacts/sigil` and left it equally exposed. A subset can declare that the cache is CDATA and never obeyed, and none of that governs what `git add -A` does with it. The hosted baseline carries the other kind of drift: a prediction written against the tree as it was before its own fix landed, in the one field no control reads.

### 🩺 Next

Add `artifacts/cache/` and `artifacts/sigil/` to `.gitignore` beside `dist/`, with the same style of comment naming why, then prove the rule by running `git check-ignore -v artifacts/cache/ask-me-questions-dtd.nt` and reading a matched line rather than exit 1. Consider a control in `lib/cache.mjs` that asserts its own `CACHE.dir` is ignored in a git work tree, so the next writer declared in a subset cannot ship without one. Correct `surfaceReported.why` in `checker/hosted-plugin.json` to predict 139 and say that a reading of 137 means the renames did not land, which turns the next upload into a test of C18 instead of a puzzle. Add `CITATION.cff` to the printed list at `checker/release-notes.mjs:216`. Give `.rot-lists/file-white.dtd`'s `figure` entry a producer or drop it. Guard the `sweep` argument with a `statSync().isDirectory()` check that refuses by name.

COMPANION VERDICT: fail
