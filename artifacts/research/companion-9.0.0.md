<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T10:46:21Z-pid2302 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Read: `checker/` (companion-run.sh whole, companion-audit.sh whole, companion-audit.dtd diff, checker-controls.sh M9 to M34), `dtd/` (law entity census across all 32 subsets, cc-sigil, cc-cache, cc-chain, typography, geometry), `dtd/sigil/` (five new documents, via the slop sweep), `lib/`, `bin/` and `checker/` engine inventory via `git ls-files`, `SECURITY.md` diff, `.gitignore` via `git check-ignore`, `artifacts/` via `git ls-files` and the ignored-file arm the runner itself reads. Ran under the wrapper: `lib/ai-slop.mjs controls` (0 failing, 117 banned phrases, 8 laws), `lib/ordinals.mjs controls` (0 failing), `lib/encoding.mjs sweep` (6 controls green, 862 files, 0 bad — no CR, no BOM), `lib/sigil.mjs controls` (15), `lib/typography.mjs controls` (35), `lib/cache.mjs controls` (20), `lib/chain.mjs controls` (26), `lib/geometry.mjs controls` (50), `checker/creators-audit.mjs --controls` (8), `checker/scala.mjs --controls` (11), `bin/adiutor.mjs controls` (31), `checker/subsets-sweep.mjs --check`, `checker/enum-sweep.mjs --check` (5619 enumerations, 0 findings), `checker/plates.mjs --check`, `checker/glossary.mjs --check`, `checker/readme-index.mjs --check`, `checker/engines-sweep.mjs`, `lib/ai-slop.mjs sweep --tracked dtd` (8 files, 0 slop). Law numbering is dense and ascending in every subset, LAW.COMPANION.7 and 8 included. `git status --porcelain` is empty and the wrapper refused nothing I asked of it.

### 🩺 Findings

<finding file="checker/companion-audit.sh" line="117" severity="high" confidence="measured">tree_state enumerates the ignored files under artifacts/research, and the raw stream is teed to $out/companion-$phase.json (line 138, line 184) during the session, between tree_before (line 143) and tree_after (line 192); git check-ignore reports .gitignore:56 artifacts/research/companion-*.json, and git ls-files --others --ignored --exclude-standard over the three directories returns exactly artifacts/research/companion-9.0.0.json, the file this run is teeing. On the first audit of any new phase name that path is absent before and present after, so the comparison at line 193 fires on the runner's own artifact: LAW.COMPANION.1 broken, the tree changed during the audit, the phase is UNAUDITED at exit 1, and the answer is discarded before the writer at line 199 ever parses it. M25 proves the mechanism by planting artifacts/cache/zz-tree-control.nt; the runner plants the same shape in artifacts/research every run. Nothing distinguishes it from a real breach.</finding>

<finding file="checker/companion-audit.sh" line="161" severity="medium" confidence="measured">The prompt commands "Write every finding as a finding element on its own lines", plural, while the scorer counts only whole-line elements: nfind at line 62 is grep -c -E '^&lt;finding .*&lt;/finding&gt;$', and M34 at checker/checker-controls.sh:249 codifies that an element split across lines is "neither counted nor faulted". A companion that reads the plural literally and writes a genuine high finding across three lines gets findings=0, and its correct fail is refused at line 93 as "a fail with no high finding" — the answer is recorded as malformed rather than as the fail it is, and the attribute check at line 68 is vacuous for that element because nsound and nfind are both zero.</finding>

<finding file="checker/checker-controls.sh" line="233" severity="low" confidence="measured">M30's coverage assertion cannot trip. n30 is incremented once per line read from bash $w --table (line 227) and want30 is wc -l of the same command in the same run (line 221), so [ "$n30" -eq "$want30" ] is a tautology and the phrase "$n30 of $want30 spellings walked" measures nothing. An engine the --table sed at checker/companion-run.sh:33 fails to extract — its pattern demands exactly six leading spaces and a name matching [a-z/.-]*, so a digit in a future engine name drops it — vanishes from both sides at once. Only the -ge 60 floor guards, and it is a constant no edit to the table updates.</finding>

### 🩺 Verdict

The shipped corpus is sound. Every control I could reach ran green with its trips landing — encoding across 862 files with the four faults each proved detectable, the slop gate red on its sloppy fixture and alive on its clean one, the sigil, typography, cache, chain and geometry laws each walked in both directions with a planted counterexample, and the four sweeps in step. Law numbering is dense and ascending in all 32 subsets, and the new LAW.COMPANION.7 and 8 follow 6 with instruments named for each. SECURITY.md's rewritten companion row matches the tree control for control; the DTD's instrument comment names M12, M16, M17, M18 to M26 and M33 to M34 and each exists where it says.

The instrument itself is what fails. Pass 23 added the tree comparison and taught tree_state to read the ignored artifact directories so a plant under artifacts/cache could not hide — and in doing so put the runner's own teed JSON inside the compared state. For phase 9.0.0 the path already exists from an earlier pass, so the guard has never fired; the next phase name will fire it on turn zero, discard a complete audit, and name the companion for a write the runner performed. A guard whose first true firing is a false positive is worse than no guard, because the reflex it trains is to disbelieve it.

### 🩺 Next

Take the raw stream out of the compared state: tee to a path outside the tree (the mktemp scratch already exists at line 137) and move it into artifacts/research after tree_after, as the record at line 214 already does; or subtract $raw and $log from tree_state's ls-files arm by name. Then plant the proof — run the runner once under a phase name whose JSON does not yet exist and confirm it scores instead of reporting UNAUDITED. Fix the prompt's "on its own lines" to the singular the scorer enforces, and either drop M30's tautological equality or give want30 an independent source, a count that an edit to the table must update by hand.

COMPANION VERDICT: fail
