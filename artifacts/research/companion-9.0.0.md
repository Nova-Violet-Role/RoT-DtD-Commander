<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Read the seventeen commits of `v8.0.0..HEAD`, the four new subsets (`dtd/cc-sigil.dtd`, `dtd/typography.dtd`, `dtd/cc-cache.dtd`, `dtd/cc-chain.dtd`), the new checkers (`checker/controls-sweep.mjs`, `checker/enum-sweep.mjs`, `checker/scala.mjs`, `checker/creators-audit.mjs`, `checker/seal-secret.mjs`, `checker/counts-sweep.mjs`), `.github/workflows/gate.yml` whole, the README claims and verify blocks, and the plates the README displays. Ran and read: `lib/ai-slop.mjs controls` (13 tells, sloppy fixture caught, clean fixture alive, shelf 19), `lib/ordinals.mjs controls` (0 failing), `bin/adiutor.mjs controls` (**31 run, 0 failing**, C31 present), `lib/sigil.mjs controls` (15), `lib/typography.mjs controls` (34), `checker/scala.mjs --controls` (11, 17 scalas for 17 families), `checker/counts-sweep.mjs --controls` (6, 40 places in step), `checker/controls-sweep.mjs --check` (23 claims, 0 drifted), `checker/enum-sweep.mjs --check` (5619 enumerations, 0 findings), `checker/contract-audit.mjs` (1928 declarations, 0 unused, 0 law gaps), `checker/readme-index.mjs --check`, `checker/glossary.mjs --check`, `checker/about-sweep.mjs`, `lib/regression.mjs unreached`, `lib/scratch.mjs controls` (11), `checker/release-notes.mjs --controls` (**10 run**), and the slop sweep over `dtd/sigil artifacts/research` (22 files, 0 slop).

Encoding: every one of the 340 changed files read as bytes in Node — 0 BOM, 0 CR, 0 control byte outside TAB and LF. Law numbering: `LAW.SIGIL`, `LAW.TYPO`, `LAW.CACHE`, `LAW.CHAIN` each 1 to 8 dense and ascending; `LAW.GEOM` 1 to 11; `LAW.FIG` 1 to 8; `contract-audit` reports 0 law gaps. `dtd/cc-sigil.dtd` tiers hold: S 33, A 28, B 27, C 24, D 13 = 125 total, 125 unique, no form in two tiers, matching `SIGIL.forms.count`. `checker/seal-secret.mjs:29` now cites `LAW.RER.6` correctly and `regression unreached` is clean over 104 files.

Disclosed breach of LAW.COMPANION.2: the portable ceiling and `< /dev/null` were carried on every command, but the allow-list grants only `node` and `git` behind it, and several read-only calls ran `grep`, `sed`, `cat`, `tail` and one `echo` under the ceiling instead. Nothing was written, edited, deleted, committed, spawned or backgrounded, and no command read stdin.

### 🩺 Findings

<finding file="docs/section-verify.svg" line="29" severity="high" confidence="measured">The plate the README displays for "🎓 Verify it yourself" contradicts the tree and the prose it claims to be made from: it draws `node bin/adiutor.mjs controls # thirty guards; C21 to C29 ...` while `node bin/adiutor.mjs controls` prints `controls: 31 run, 0 failing` and `README.md:571` reads `thirty-one guards; C31 the gate law of cc-cache at Stop`. C31 is new in this phase, added with `dtd/cc-cache.dtd` (`LAW.CACHE.5` names it), so the number went stale inside 9.0.0. Two claims fall with it: the plate's own header says `GENERATED from README prose` and its last commit is `eaf7bb5` at 7.1.3, and `README.md:570` states `Nothing here is hidden that the plate does not also show` while the plate omits C31 and shows a different count. No instrument reads it — `git grep -n section-verify -- checker lib package.json .github` exits 1, and `checker/counts-sweep.mjs` reads the guard count out of `README.md` only, so its `40 places in step` is green over a published artifact that drifted.</finding>

<finding file="README.md" line="534" severity="high" confidence="measured">The Adiutor claims row publishes `node bin/adiutor.mjs controls`: `20 run, 0 failing`, dated 2026-09-03; the instrument prints `controls: 31 run, 0 failing` today. `README.md:550` claims that every count the repository publishes, the claims rows named among them and the Adiutor guards named among the counts, equals the tree, so one README line falsifies another. `checker/counts-sweep.mjs` reads the amplify, list, build, checker-controls and contract-audit claims rows but has no place entry for this one, and its own comment at line 121 says a row that no longer re-runs is what the sweep exists to refuse. `docs/claims-map.svg:33` republishes the stale number as `adiutor.mjs controls: 20 run, 0 failing`, and that plate is what a reader sees at `README.md:521`.</finding>

<finding file="README.md" line="554" severity="medium" confidence="measured">The release claims row publishes `node checker/release-notes.mjs --controls`: `4 run, 0 failing`; the command prints `release-notes controls: 10 run, 0 failing`. Same gap as the row above: no `places()` entry in `checker/counts-sweep.mjs` matches `release-notes\.mjs --controls`, so the number is published and read by nothing.</finding>

<finding file=".github/workflows/gate.yml" line="64" severity="medium" confidence="measured">The gate step name says `adiutor controls (thirty guards tripped on purpose; C21 to C29 ...)` against a tree that measures 31. This file is already in the sweep's scope — `checker/counts-sweep.mjs:137` reads the checker-controls step name of this same workflow and holds three numbers in it — so the omission is one step name, not one file. The step also stops at C29 while C30 and C31 are the two guards this release cares about.</finding>

<finding file="docs/section-verify-dark.svg" line="29" severity="medium" confidence="measured">The dark plate carries the identical stale line, `node bin/adiutor.mjs controls # thirty guards; C21 to C29 ...`, and is served by `README.md:564` to every reader whose browser reports a dark colour scheme. Fixing only the light plate leaves half the readers on the wrong number.</finding>

<finding file=".github/workflows/gate.yml" line="299" severity="low" confidence="reasoned">In the `scala` job, `rc=$?` on line 299 cannot be reached when line 298 fails. A step declaring `shell: bash` runs under `bash --noprofile --norc -eo pipefail`, so a nonzero exit from `node checker/scala.mjs run --all` aborts the step at once: `ls -l "$O"` on line 300 never runs and the artifact listing the step exists to print is lost on exactly the run that needs it. The release job at line 348 shows the guard this one is missing — it wraps its capture in `set +e` and `set -e` around `rc=$?`.</finding>

### 🩺 Verdict

The engines of this phase hold. Every instrument I was told to run came back green, and so did every instrument I ran beyond them: the sigil census closes in both directions at 125 forms, typography spends all 36 of its entities, the four new law families are dense and ascending, the contract audit finds no unused declaration, the enumeration sweep resolves 5619 enumerations, and 340 changed files carry no CR, no BOM and no stray control byte. The seventh pass's repairs landed — `checker/seal-secret.mjs:29` cites `LAW.RER.6`, the scala census reports 17 scalas for 17 families with the Adiutor resolved, and `controls-sweep --check` reads 23 changelog claims with none drifted.

What fails is the layer outside the engines. Adding C31 moved a number that six published places repeat, and the sweep built to catch exactly that reaches four of them. Two of the misses are the SVG plates a README reader actually looks at, and both declare in their own header that they were generated from prose they no longer match; a third is a claims row the README's own text promises equals the tree. The pattern is one hole, not five: an artifact that is published, drawn from a measured number, and read back by nothing. `checker/glossary.mjs` and `checker/readme-index.mjs` already render and re-check their plates, so the shape of the fix exists in the tree; the `docs/section-*.svg` family and `docs/claims-map.svg` were never given the same treatment.

### 🩺 Next

Add the missing places to `checker/counts-sweep.mjs`: `README.md` `adiutor\.mjs controls`: `(\d+) run` against `c.guards`, `README.md` `release-notes\.mjs --controls`: `(\d+) run` against the measured total, and the `adiutor controls \(([a-z-]+) guards` step name of `.github/workflows/gate.yml` — then bump the two published `places in step` counts, which the sweep already holds against `p.length + 2`. Bring the plates under an instrument rather than under a hand: give `docs/section-*.svg` and `docs/claims-map.svg` a renderer with `--check`, the way `checker/glossary.mjs:561` holds `glossary-map.svg`, so the header claim `GENERATED from README prose` is a fact a command re-derives. Correct `README.md:534` to 31 and `README.md:554` to 10 first, since the plates are drawn from those rows. Print the landed proof before trusting the repair — plant `thirty` back into the README verify line and confirm `counts-sweep` names it, then restore and confirm `40 places in step` returns with the new total. Last, wrap line 298 of the scala job in `set +e` and `set -e` so `rc=$?` and the `ls -l "$O"` beneath it survive a red run.

COMPANION VERDICT: fail
