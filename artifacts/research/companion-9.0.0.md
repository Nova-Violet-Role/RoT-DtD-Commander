<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T07:24:48Z-pid468 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (`companion-audit.sh` whole 1-185, `companion-audit.dtd` diff, `checker-controls.sh` whole 1-146, `spdx-sweep.sh` whole 1-118, `counts-sweep.mjs` 30-80 and 155-215, plus `contract-audit.mjs`, `gate-sync.mjs`, `plates.mjs --check` run); root files `.gitignore` (whole, plus `git check-ignore -v` on `.rot-moe`, `.rot-moe/foo.jsonl`, `artifacts/research/.rot-moe`), `REUSE.toml` (whole), `README.md` (badges 21-27, claims rows 584-600, verify 647-650), `CHANGELOG.md` (182-186, 526-580, 638-644), `.claude-plugin/plugin.json`, `.github/workflows/gate.yml` (1-80); `dtd/sigil/greek-numbers.md` (whole 1-205); `lib/` (`ai-slop.mjs`, `ordinals.mjs`, `typography.mjs` 296-313 via its controls); `artifacts/research/` (the tracked record, whole diff); `docs/` (the four re-rendered svg plates, by grep); and a walk of the whole work tree for nested `.gitignore` files.

Instruments run, all read-only: `lib/ai-slop.mjs controls` prints `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8` with every landed proof shown; `lib/ordinals.mjs controls` prints `ordinals controls: ok (0 failing)`. `counts-sweep.mjs`: `commands 139, skills 22, agents 5, checked 166, guards 31, checker controls 25, declarations 1928; 62 places in step`. `contract-audit.mjs`: 1928 declarations, 0 unused, 0 law gaps, four controls fired. `gate-sync.mjs`: 83 in the chain, 0 missing, three controls fired. `plates.mjs --check`: 16 plates in step. `lib/typography.mjs controls`: 34 run, 0 failing. `ai-slop.mjs sweep --tracked dtd/sigil artifacts/research --max 0`: 21 files, 0 slop, matching the changelog row; `ai-slop.mjs CHANGELOG.md` and `README.md`: both `alive=yes`, every measure holding. Encoding re-derived in node over all 866 tracked files: CR 0, BOM 0, control bytes 0, no-final-LF 0, the only hits being the five tracked GIFs. The new foreign-ignore arm simulated in node over the tree: one nested `.gitignore`, `.rot-moe/.gitignore`, untracked, its directory named by `.gitignore:47`, so the arm is green and the changelog's claim holds. `git status --porcelain` empty before and after; nothing here wrote.

One claim I built and then killed with a measurement, recorded so it is not re-derived: `companion-audit.sh:107` turns `$out` into an MSYS path and hands it to `node -e` as argv, which looked like the `C:\c\...` ENOENT this session hit on its own first command. It is not a defect — MSYS converts a standalone argv path, measured: `node -e ... /c/.../REUSE.toml` reports `argv1=C:/CLAUDE_CODE_COMMANDER/RoT_DtD_Commander/REUSE.toml` and reads 1409 bytes.

### 🩺 Findings

<finding file="checker/companion-audit.sh" line="153" severity="high" confidence="measured">The allow-list is `Read,Grep,Glob,Bash(node $here/lib/ceiling.mjs 60 node:*),Bash(node $here/lib/ceiling.mjs 60 git:*)`, and the comment at lines 31 to 37 states that this shape IS LAW.COMPANION.1 and LAW.COMPANION.2, "enforced by this shape rather than by a string the runner checks". A `Bash(<prefix>:*)` rule admits any command starting with that prefix, so the granted forms are a general-purpose interpreter and a version-control write tool: `node ... 60 node -e "require('fs').writeFileSync(...)"`, `...rmSync`, `...child_process.spawn({detached:true})`, and `node ... 60 git commit`, `git checkout --`, `git clean -xdf` all match and are auto-approved. LAW.COMPANION.1 forbids exactly those verbs by name — writes, deletes, commits, spawning, backgrounding. The breach is not hypothetical: the record this phase replaced disclosed it, and the line is in this range's own diff, `-Breach to disclose under LAW.COMPANION.1: checker/glossary.mjs was run and it writes — it re-rendered the xhtml page, 40 svg plates and the README block.` The only control is `allow_ok` at checker/checker-controls.sh:116-127, which tests three things: the strings `Write|Edit|NotebookEdit|Agent|Task`, a bare `Bash`, and the ceiling prefix on every `Bash(...)` form. None of the three can see that the binary behind the ceiling is an interpreter, so M17, M17b and M17c pass on an allow-list that grants everything the law forbids. The law is honoured today only by the companion's own restraint, which is the state the DTD's instrument map says it is not in.</finding>

<finding file="checker/companion-audit.dtd" line="58" severity="medium" confidence="measured">The instrument map assigns every law an instrument and gives LAW.COMPANION.5 only "the runner's exit 124 branch" — a branch, where 3, 4, 6, 7 and 8 each name M-controls and 1 and 2 name M17. Reading checker/checker-controls.sh whole, no control mentions 124, plants a ceiling that fires, or asserts the UNAUDITED path; the suite's 25 runs are M0 to M22 with M17b and M17c, none of them that branch. The one law that decides whether an unfinished audit is recorded as unaudited rather than passed is the one law with nothing that trips it, and companion-audit.sh:159 is a single `if` that no test has ever entered.</finding>

<finding file="checker/spdx-sweep.sh" line="21" severity="medium" confidence="measured">The comment above `globs` still reads "A leading **/ means any depth including the root, which a bash case pattern says as two arms" — it describes the `case "$f" in $p|*/$p` implementation this phase deleted, while the code fourteen lines below is a sed-built regex. Worse, the arm it describes is exercised by nothing: every one of the nine globs in REUSE.toml:21 is literal-prefixed (`.claude-plugin/*.json`, `checker/*.json`, `dtd/*.json`, `monitors/*.json`, `package.json`, `skills/*/templates/*.json`, `src/skills/*/templates/*.json`, `docs/tapes/*.jsonl`, `docs/gifs/*.gif`), so the `s|__ANY__|(.*/)?|g` substitution never fires on real input, and the four planted control files at lines 71 to 76 exercise only the `[^/]*` arm. The translation's any-depth half is untested code carrying a comment that documents its predecessor.</finding>

<finding file=".gitignore" line="43" severity="medium" confidence="reasoned">`**/artifacts/**/.rot-moe/` was kept when `.rot-moe/` was added four lines below it. A gitignore pattern with no internal slash matches a directory of that name at any depth, so line 47 covers every path line 43 covers and strictly more; there is no input that line 43 alone can reach. Measured for the root case: `git check-ignore -v .rot-moe` answers `.gitignore:47:.rot-moe/`, and `.gitignore:43` is reported for nothing. A rule that cannot be the reason a path is ignored reads to the next maintainer as a scope the repository needs, and invites the same narrow-then-general repair the seventeenth pass had to make.</finding>

<finding file="lib/typography.mjs" line="308" severity="medium" confidence="measured">The repaired control reads exactly one row, `/\|\s*29\s*\|\s*\*\*([a-z]+)-\*\*/`, so the table-versus-constructor agreement it now claims is proved for 29 and asserted for nothing else. I parsed all 61 prefix rows of dtd/sigil/greek-numbers.md and compared each to `numeral(n)`: 45 rows are inside the declared bound and every one agrees, 16 diverge only because they are past it and correctly come back `form=plain` (200 through 999). So the whole table can be checked, cheaply, in the loop that already exists — and the control instead pins the single row the last pass happened to find. That is the shape the seventeenth pass's own verdict named, a guard aimed at last week's example rather than at what the example was an instance of, reproduced in the fix for it.</finding>

<finding file="README.md" line="591" severity="low" confidence="measured">The row's evidence column was rewritten at HEAD — `twenty-four controls M0 to M21` to `twenty-five controls M0 to M22`, and `checker controls: 22 run` to `25 run` — and counts-sweep.mjs:203 was added in the same commit to hold that quoted string to the instrument. The "last measured" column still reads 2026-09-06. Rows 588, 589, 590, 592 and 593 around it read 2026-09-07. A date column that does not move when the number beside it is corrected tells a reader the row was checked before the change that changed it.</finding>

<finding file="checker/companion-audit.sh" line="85" severity="low" confidence="measured">On the pass branch, line 80 returns 1 when the headings are wrong; on the fail branch, line 85 prints the identical LAW.COMPANION.8 message and then falls through to `return 1` for the fail itself, so the heading breach changes nothing about the exit code and is indistinguishable from a plain fail. The law's wording, "a pass without them is not a pass", licenses that. But the only control, M21, plants a pass record, so the fail-path line is prose no test reaches, and a companion answering fail with no Scope heading at all is scored exactly as one that answered in the declared grammar.</finding>

<finding file="checker/spdx-sweep.sh" line="93" severity="low" confidence="reasoned">`find . -name .gitignore -not -path './node_modules/*'` filters the output but does not prune the walk, so both `foreign()` calls descend `.git/` and the whole of `node_modules/` in full — two complete tree walks per sweep, on all three legs of gate.yml:34. The exclusion is also top-level only: a nested `a/node_modules/b/.gitignore`, which npm produces routinely, is not matched by `./node_modules/*` and would be named as a foreign ignore, failing the sweep on a file no tool of this repository wrote. `-prune` on `.git` and any `node_modules` fixes both at once.</finding>

### 🩺 Verdict

The seven repairs the seventeenth pass asked for all landed, and I checked each against the tree rather than the changelog. The foreign-ignore arm now walks from the root: simulated over this tree it finds one nested `.gitignore`, `.rot-moe/.gitignore`, and skips it because `.gitignore:47` reaches the directory — `git check-ignore -v .rot-moe` answers `.gitignore:47:.rot-moe/`, so the changelog sentence at 568 to 570 is one command away from being disproved and survives it. The Greek table writes `icosiennea-` and agrees with the constructor on all 45 rows inside the bound. `${globs[0]:-}` replaces the array-length expansion, the `--score` guard exits 2 as the header promised, `artifacts/research/companion-*.md.tmp` is ignored, and the README row quotes 25 with counts-sweep.mjs:203 holding it — 62 places in step, and the plates re-rendered to match. Encoding is clean across all 866 tracked files by byte count, no law prefix skips a number, and both mandated control suites print every landed proof.

What fails is one layer under all of that. The runner's allow-list is the instrument that makes every companion answer trustworthy, and the file states in a comment that the allow-list *is* laws 1 and 2. It is not. `node:*` behind a ceiling is a general interpreter and `git:*` behind a ceiling is a commit-and-delete tool, and the control that guards them inspects tool names and a prefix string, which is the one thing about that grant that is already correct. The evidence that this is not theoretical is in this range's own diff, where a previous companion disclosed running a writing script under exactly that grant. The pattern the phase should carry forward is the one its own seventeenth verdict wrote down and then repeated twice: the typography repair pins row 29 rather than the table, and the `**/` translation arm was written for a glob shape REUSE.toml does not contain. A guard is worth what its widest input proves, not what last week's instance did.

### 🩺 Next

Close the allow-list first. Either narrow the grants to the executables the audit actually needs — a wrapper script under `checker/` that the companion may run, rather than `node` and `git` themselves — or stop claiming the shape enforces laws 1 and 2 in companion-audit.sh:31-37 and in the DTD map, and add the check that does: a post-run `git status --porcelain` and a `git rev-parse HEAD` taken before and after the nested session, compared by the runner, with a planted control that writes a byte in the scratch and requires the runner to go red. Extend `allow_ok` in checker-controls.sh so a copy granting `node:*` or `git:*` with no wrapper is refused, and plant that copy as M23. Give LAW.COMPANION.5 a control: score a run whose ceiling fired and require exit 124 with the phase recorded unaudited, so the one law that separates unfinished from passed is not the only law with no test. Turn lib/typography.mjs:308 into a loop over every prefix row of dtd/sigil/greek-numbers.md, asserting agreement inside the bound and `form=plain` past it — 61 rows, one pass, no row left unpinned. Either add a `**/`-prefixed annotation to REUSE.toml or delete the `__ANY__` arm and the comment at spdx-sweep.sh:20-21 that documents the deleted `case` implementation. Drop `.gitignore:43`, now that :47 subsumes it. Add `-prune` for `.git` and any `node_modules` to the `find` at spdx-sweep.sh:93, and match nested `node_modules` rather than only the top-level one. Refresh the date on README.md:591 to the day its evidence changed.

COMPANION VERDICT: fail
