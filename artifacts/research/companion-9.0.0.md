<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T09:56:41Z-pid1026 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (`companion-run.sh` whole, `companion-audit.sh` whole, `checker-controls.sh` lines 1-100 and 129-239, `counts-sweep.mjs` by grep, `companion-audit.dtd` as given), repository root docs (`SECURITY.md` 14-35, `README.md` and `CHANGELOG.md` by grep), `.github/workflows/gate.yml` (the checker-controls step), `src/skills/dtd-core-dtd/references/` (`subsets.md` through the slop gate), and `lib/` + `bin/` exercised as engines through the wrapper.

Run through `checker/companion-run.sh` only, every command under the wrapper's 300 s ceiling with stdin closed: `lib/ai-slop.mjs controls` (0 failing, 117 banned phrases, 8 measures, 8 laws), `lib/ordinals.mjs controls` (0 failing), `lib/typography.mjs controls` (35 run, 0 failing), `lib/encoding.mjs sweep` (862 files, 0 bad; controls 6 run, 0 failing), `bin/rot-dtd-commander.mjs build --check` (303 targets, 0 drifted), `checker/subsets-sweep.mjs --check` (28 blocks in step), `companion-run.sh --table` (69 pairs), `lib/ai-slop.mjs` on `src/skills/dtd-core-dtd/references/subsets.md` and `SECURITY.md` (both `alive=yes`), `lib/cache.mjs load`, `lib/schematic.mjs check`, and `git log`/`git show` over the range. Nothing was written; no command was retried after a refusal.

### 🩺 Findings

<finding file="SECURITY.md" line="25" severity="high" confidence="measured">The security surface table states the companion's "tool allow-list carries no writing or spawning tool and every Bash form starts with `timeout 60`". The tree contradicts both halves of the second clause: the allow-list at checker/companion-audit.sh:176 is `Read,Grep,Glob,Bash(bash $here/checker/companion-run.sh:*)` with no `timeout` in it, and the ceiling the wrapper applies is 300 s (checker/companion-run.sh:148 and :165), not 60. Worse, control M17c at checker/checker-controls.sh:154-157 now REFUSES a runner copy whose Bash form is `Bash(timeout 60 node:*)` — SECURITY.md names as the safeguard the exact spelling the checker rejects. `git log v8.0.0..HEAD -- SECURITY.md` returns nothing while a188d9f and b97d0a8 rewrote the allow-list twice in this range, and no engine under checker/ reads SECURITY.md (grep over checker/ for the filename: no matches), so no control can trip on it. A reader auditing the sandbox is told to verify a string that is absent and that the suite would reject if restored.</finding>

<finding file="checker/companion-run.sh" line="71" severity="medium" confidence="measured">The table's comment claims "each spelling measured to exit without a usage line and to leave the tree as it was". Measured against the table itself: `bash checker/companion-run.sh node lib/schematic.mjs check` (granted at line 90) prints `usage: node lib/schematic.mjs render <schema> <form> | ...` and exits 2, and `node lib/cache.mjs load` (granted at line 87) prints `cache: a command name that is not a name: undefined` and exits 1. Two of the 69 granted spellings are not reading spellings; the comment records a measurement the tree does not support.</finding>

<finding file="checker/checker-controls.sh" line="221" severity="medium" confidence="measured">M30 records a spelling as bad only on `[ $rc -eq 3 ]`, and the comment at line 210 excuses 124 and an engine's own 1 or 2. That is exactly the property the table at checker/companion-run.sh:71 claims was measured, so the control cannot trip on it: `lib/schematic.mjs check` exits 2 with a usage line and `lib/cache.mjs load` exits 1, and M30 walks both green. A spelling that hangs to the wrapper's 300 s ceiling is likewise excused, so the walk cannot distinguish a granted engine that reads from one that never returns.</finding>

<finding file="checker/checker-controls.sh" line="228" severity="medium" confidence="measured">M30 guards its walk with `[ $n30 -ge 40 ]` while `companion-run.sh --table` emits 69 pairs (counted from the run above). The table is generated by a sed at checker/companion-run.sh:33 that requires exactly six leading spaces and a single-alternative case label; any grant line reindented or folded into an alternation drops out of `--table` silently. Up to 29 grants can vanish from the walk and M30 still reports PASS with a lower spelling count in its own message, which is the one number a reader would have to notice by hand.</finding>

<finding file="checker/checker-controls.sh" line="30" severity="medium" confidence="measured">The recursion guard added by eddd8d8 has no control. `ROT_CHECKER_CONTROLS_RUNNING` appears exactly three times in the whole repository (lines 30, 31 and 34 of this file), all inside the guard itself — nothing plants the mark and asserts the exit-3 refusal, so the fix for the 521-process fork is the one thing in this suite that has never been tripped on purpose. It also exits 3, the same code checker/companion-run.sh:29 uses for a wrapper refusal, so if a granted engine ever forks the suite through any path, M30's `rc -eq 3` test at line 221 reports it as `[spelling]=refused` and names the wrong cause.</finding>

<finding file="checker/companion-audit.sh" line="113" severity="low" confidence="reasoned">`tree_state()` returns the string `tree_state: git status failed five times` on failure, and both callers capture it as data (`tree_before` at line 136, `tree_after` at line 185) without reading the return code; there is no `set -e`. If both readings fail the two strings compare equal and a write performed during the audit passes the LAW.COMPANION.1 comparison undetected; if one fails, a clean audit is reported as a breach and the phase is called UNAUDITED. The five-try retry makes it unlikely, not impossible, and the failure is silent in the direction that matters.</finding>

<finding file="SECURITY.md" line="25" severity="low" confidence="measured">The row's "verified by" cell names control M17 alone. The range added M23 (the old prefix grant refused), M24 (the wrapper's refusals), M25 (the tree compared around the session), M26 (the fired ceiling), M27 to M30 (git config, the engine table, the bare arms, the table walk) and M31 (`--permission-mode default`, which stops a parent session in bypass mode widening the nested grant). The evidence column understates the controls actually in force by nine, so the document reads as weaker than the checker is.</finding>

### 🩺 Verdict

The engineering of this phase holds. Every instrument I could run under the wrapper is green and internally consistent: 862 files with no CR, BOM or missing final LF; 303 build targets with none drifted; 28 quoted subset blocks in step; 35 typography controls and 8 slop laws with nothing failing; the largest changed document, `src/skills/dtd-core-dtd/references/subsets.md` at +1542 lines, passes the AI_SLOP gate `alive=yes` with `tells=0` and `static_share=0.0141`. The commit message of eddd8d8 claims "34 controls, 69 spellings walked" and both numbers reconcile against the tree: `--table` printed 69 pairs, and the control labels in `checker-controls.sh` sum to 34, which is what README.md:591 and README.md:650 publish (34, span M0 to M31, 34 − 7 − 2 = 25 scorer and runner controls). LAW.COMPANION.1 through 8 are numbered in sequence and each is held by the instrument its comment names.

The fail rests on one thing, and it is the thing this repository's own discipline says matters most. SECURITY.md is the document a stranger reads before running anything here, and it describes a sandbox that no longer exists. It tells that reader to check for `timeout 60` in the companion's allow-list. There is no `timeout` there; the ceiling is 300 s; and the suite added a control this cycle whose entire job is to refuse the form the document recommends. A reader who trusts the page and "repairs" the runner to match it would write the M17c mutation by hand and turn the gate red. The five passes in this range rebuilt the companion's sandbox twice over and left its security page untouched, and nothing in checker/ reads that file, so the drift had no way to surface. The remaining findings are a matched pair — a comment in `companion-run.sh` recording a measurement two of its own 69 grants fail, and the M30 walk that excuses exit 2 by design so the claim can never be checked — plus a recursion guard shipped with no control, a walk floor set at 40 against a table of 69, and a tree-state helper that returns its error message as a value.

### 🩺 Next

Bring SECURITY.md:25 to the tree: replace the `timeout 60` clause with the wrapper form actually granted at companion-audit.sh:176, the 300 s portable ceiling, and `--permission-mode default`; extend the evidence cell to M17, M17b, M17c, M23 to M31 and the tree comparison. Then give the row a control it cannot outlive — a claims entry in counts-sweep.mjs reading the allow-list string out of companion-audit.sh and the ceiling out of companion-run.sh, so the next allow-list rewrite goes red on the security page instead of past it. Second, close the pair: drop `lib/schematic.mjs check` and `lib/cache.mjs load` from the table, or fix M30 to fail on any exit other than 0 and the engine's declared verdict codes, so the comment at companion-run.sh:71 becomes a measurement again. Third, plant `ROT_CHECKER_CONTROLS_RUNNING=1` and assert exit 3 with its named line as M32, and raise M30's floor to the count `--table` actually emits rather than 40.

COMPANION VERDICT: fail
