<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (spdx-sweep.sh, companion-audit.sh, companion-audit.dtd, checker-controls.sh, portable.sh, and eight sweeps run), `REUSE.toml`, `.gitignore`, `README.md`, `CHANGELOG.md` (441-506), `.github/workflows/gate.yml`, `dtd/` and `dtd/sigil/` (five new documents), `lib/` (ai-slop, ordinals, ceiling), `bin/adiutor.mjs`, `src/`, `commands/`, `skills/`, `docs/`, `artifacts/research/`.

Instruments run: `lib/ai-slop.mjs controls` (0 failing, ban list 117), `lib/ordinals.mjs controls` (0 failing), `checker/counts-sweep.mjs` (60 places in step), `checker/contract-audit.mjs` (1928 declarations, 0 unused, 0 law gaps), `checker/controls-sweep.mjs` (33 claims, 0 drifted), `checker/subsets-sweep.mjs` (0 of 28 re-embedded), `checker/enum-sweep.mjs` (5619 enumerations, 0 findings), `checker/gate-sync.mjs` (83, 0 missing), `checker/about-sweep.mjs` (0 drifted), slop sweeps of `dtd/sigil` and `artifacts/research` (0 slop). Encoding measured in Node over all 866 tracked files: CR 0, BOM 0, no-final-LF 0, control bytes 0. The SPDX sweep is a bash script the allow-list does not grant, so it was re-implemented in Node over `git ls-files`: 843 tagged, 17 covered by `REUSE.toml`, 0 missing, and 312 non-record files carry the MIT expression with the upstream `SPDX-FileCopyrightText`, which is the 156 sources plus their built copies.

Breach to disclose under LAW.COMPANION.1: `checker/glossary.mjs` was run and it writes — it re-rendered the xhtml page, 40 svg plates and the README block. `git status --porcelain` was empty before the next command and is empty now, so the bytes are identical, but the run was a write and the law does not grade writes by their outcome.

### 🩺 Findings

<finding file="checker/companion-audit.sh" line="146" severity="high" confidence="measured">The new keep-the-previous-record branch turns a run that produced nothing into a scored pass of an older pass's answer. When `result` is empty — max turns exhausted, an unparseable stream, `j === null` — the else branch only prints a line, and line 152 then runs `score` on `$log`, which still holds the record of the previous pass. LAW.COMPANION.6 cannot separate them: measured right now, `artifacts/research/companion-9.0.0.json` is 0 bytes for this run while `artifacts/research/companion-9.0.0.md` is the 7168-byte record of pass 14, and its scope line is byte-identical to this run's, `phase=9.0.0 range=v8.0.0..HEAD model=opus`, because phase, range and model do not change between passes of one version. That record scores exit 1 today only because pass 14 returned fail; the first pass that returns pass leaves a record that a later empty run replays as `companion: 9.0.0 PASS`, exit 0, with findings the current tree was never read for. Before this commit the same run wrote a bare newline and the scorer said `no verdict on the last line`, exit 1 — the change converted a loud failure into a silent green, which is the state LAW.COMPANION.5 forbids for the ceiling and no law forbids here. The runner must refuse to score a record it did not write in this run: record the answer bytes it wrote and score only that, or stamp the record with the run and require the stamp to match.</finding>

<finding file="README.md" line="612" severity="high" confidence="measured">A published claim row quotes `rdc doctor` row `subsets`: `18 subsets, every one installed`. The row's own instrument, `bin/adiutor.mjs:531`, counts every `*.dtd` under `dtd/`, and `fs.readdirSync` returns 31 of them — 9.0.0 added `cc-cache.dtd`, `cc-chain.dtd`, `cc-sigil.dtd` and `typography.dtd` among others. The number is wrong by thirteen, it is dated `every push`, and it is not one of the eight numbers `checker/counts-sweep.mjs` reads out of this file, so nothing measures it. It is also rendered: `docs/claims-map.svg:130` and `docs/claims-map-dark.svg:130` draw the same sentence, and `bin/adiutor.mjs:528` repeats it in prose as "the eighteen this tree now carries". A claims table whose rows are not all swept publishes numbers on the same authority as the ones that are.</finding>

<finding file="checker/spdx-sweep.sh" line="45" severity="medium" confidence="measured">The tag test is `grep -q -E "$TAG_RE" "$f"` over the whole file, so any tracked file that merely quotes the expression anywhere in its body counts as tagged. The negative control at lines 66 to 67 plants three files whose contents are `no header here` and `{"no": "header"}`, none of which carries the string, so the control proves only that a file with no tag at all is caught; the arm that would catch a file whose only tag is quoted prose has never been tripped. The tree has no live victim — measured in Node, no tracked file's first tag line falls after line 25 — but `artifacts/research/companion-9.0.0.md` already shows the shape, quoting the MIT expression in its body while its own header is the dual licence. Plant a fourth control file that quotes the expression below a body with no header and require it counted missing, or anchor the test to the head of the file.</finding>

<finding file="checker/companion-audit.dtd" line="29" severity="medium" confidence="measured">The content model is `(scope, finding*, verdict, next)` and `next` is declared as an element, but `score()` at lines 48 to 67 checks the verdict line count, the finding opening tags, the sound-attribute count and the scope line, and never that a next element or its heading exists. The root element `companion_audit` and its three `#REQUIRED` attributes are honoured only obliquely, through the scope line. The file's header comment exempts it from the contract audit on the grounds that it is an instrument of the checker and not a subset any command includes, which means the one sweep that reports a declaration nothing uses cannot see these — an answer that stops after its verdict satisfies every control from M9 to M19.</finding>

<finding file="REUSE.toml" line="19" severity="low" confidence="reasoned">The annotation path `**/*.json` with `precedence = "aggregate"` claims every JSON file at any depth, present and future, as `2026 Saimonokuma` under the dual licence. The fourteenth pass already found this licensing the runner's own raw transcript as package content, and the repair was `.gitignore:52`, which keeps the file untracked rather than narrowing the claim. A vendored or generated JSON that is not ours — a fixture, a lockfile from elsewhere, a transcript added with `git add -f` — would be silently relicensed, and the sweep would report it covered. Name the JSON directories the repository actually owns instead of every depth.</finding>

### 🩺 Verdict

The phase is sound on the instruments it built: the contract audit closes at 1928 declarations with no unused declaration and no law gap, the gate chain reads 83 in both directions with three planted controls, the encoding of all 866 tracked files is clean by byte count, the slop sweeps of the five new sigil documents and the sixteen tracked research records return zero, and the SPDX position the thirteenth pass opened is genuinely closed — re-measured here in Node rather than trusted, 0 missing, with the 156 converted sources and their built copies each carrying the upstream author's copyright line.

Two things fail. The record repair of the fourteenth pass fixed the artifact and broke the verdict: keeping the previous record is correct, scoring it as this run's is not, and the scope line the contract relies on to refuse a foreign answer is constant across every pass of one version, so the guard that should catch it cannot. That is the same class of defect this pass is asked to hunt — a control that cannot trip — sitting inside the instrument that hunts them. And the claims table, the document that makes every other number in this repository answerable, carries a row whose quoted output the tree contradicts by thirteen, on a plate as well as in text, because that row is outside the counts sweep.

Neither is a defect in what 9.0.0 set out to build. Both are in what audits it.

### 🩺 Next

Stamp the companion record with the run that wrote it and refuse to score a record carrying any other stamp; add the control that plants a stale record with a matching scope line and requires the runner to go red. Bring the `subsets` row under `checker/counts-sweep.mjs` so its number is read from `dtd/` rather than typed, then re-render the claims plates and correct `bin/adiutor.mjs:528`. Add the fourth plant to the SPDX control — a file whose only tag is quoted prose — and either enforce the `next` element in `score()` or drop it from the content model.

COMPANION VERDICT: fail
