<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T07:06:01Z-pid676 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (companion-audit.sh whole 1-184, companion-audit.dtd diff, checker-controls.sh 24-146, spdx-sweep.sh whole, counts-sweep.mjs, subsets-sweep.mjs 60-131, plus gate-sync, contract-audit, readme-index, controls-sweep run), root files `REUSE.toml`, `.gitignore` (whole, plus `git check-ignore -v`), `README.md` (badges 22-27, index 199-202, claims rows 584-613, verify 637-650), `CHANGELOG.md` (515-551, plus greps), `package.json` (53-70, 145-146); `.github/workflows/gate.yml` (34-35, 68-71, 147-151, 211-312); `dtd/` and `dtd/sigil/` (greek-numbers.md 50-71, slop sweep over all five); `lib/` (ai-slop, ordinals, typography 296-313, and a write-site census of cache/chain/figure/geometry/sigil); `artifacts/research/` (the tracked record whole, and the directory walked on disk); `src/commands` and `commands/`, `src/skills` and `skills/`, `src/agents`, `docs/` (byte-level parity and tag census, 864 files scanned).

Instruments run, all read-only: `lib/ai-slop.mjs controls` prints `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8` with every landed proof shown; `lib/ordinals.mjs controls` prints `ordinals controls: ok (0 failing)`. `contract-audit.mjs`: 1928 declarations, 0 unused, 0 law gaps, four controls fired. `counts-sweep.mjs`: commands 139, skills 22, agents 5, checked 166, guards 31, checker controls 25, declarations 1928, 61 places in step. `gate-sync.mjs`: 83 commands in the gate chain, 0 missing. `readme-index.mjs --check`: in step. `controls-sweep.mjs --check`: 33 claims, 0 drifted. `lib/typography.mjs controls`: 34 run, 0 failing. Slop on README, CHANGELOG, `dtd/sigil` and `artifacts/research`: alive, 0 slop. Encoding re-derived in node over 864 files: 0 CR, 0 BOM, 0 stray control bytes. SPDX coverage re-derived in node against the REUSE.toml globs: 843 tagged, 17 covered, every uncovered path ignored or untracked. `git status --porcelain` is empty after every run: nothing here wrote.

### 🩺 Findings

<finding file="checker/spdx-sweep.sh" line="87" severity="high" confidence="measured">The foreign-ignore arm added this pass is `foreign() { find artifacts -mindepth 2 -name .gitignore ...; }`, and the live instance of the defect it was written for is at the repository root, outside its reach. Measured now: `C:/CLAUDE_CODE_COMMANDER/RoT_DtD_Commander/.rot-moe/` holds three files (`animus-distillate.md`, `rot-route-f30aac10-57d4-40b3-a32e-585363cae815.jsonl`, `.gitignore`), and `git check-ignore -v .rot-moe/foo.jsonl` names `.rot-moe/.gitignore:1:*` as the only rule reaching it, while the same command on `artifacts/research/.rot-moe/foo.jsonl` names the repository rule `.gitignore:43`. Simulating the arm's own find gives an empty result, so `f0` is empty and the sweep exits green. Failure: the plugin rewrites or drops its two-byte `.gitignore`, or a clone omits it, and the next `git add -A` stages raw session transcripts into a public AGPL repository; `git status --porcelain` prints clean today and the arm that exists to warn cannot see the directory. The control at line 89 plants under `artifacts/` and therefore trips on the only case the arm can reach.</finding>

<finding file="CHANGELOG.md" line="543" severity="high" confidence="measured">The 9.0.0 entry states "`.rot-moe/` is ignored wherever a session ran and the droppings are gone". The tree contradicts both halves. `.gitignore:43` is `**/artifacts/**/.rot-moe/`, which requires an `artifacts` path component; the repository names `.rot-moe` nowhere else, confirmed by reading the whole file. `git check-ignore -v .rot-moe/foo.jsonl` returns `.rot-moe/.gitignore:1:*`, the plugin's own file, so at the repository root, where the build session runs, the directory is ignored by exactly the mechanism the sixteenth pass set out to stop depending on, and three of its files are on disk. A reader of the changelog concludes the leak is closed and stops looking; the concealment is still owned by the tool, not by the repository.</finding>

<finding file=".gitignore" line="43" severity="medium" confidence="measured">The rule added in this phase is `**/artifacts/**/.rot-moe/`, scoped to a path containing `artifacts`. The runner it was written for now runs its nested session in `mktemp -d` outside the tree (checker/companion-audit.sh:110), so the artifacts case can no longer be produced, while the case that does exist, a session whose working directory is the repository root, is uncovered. A one-line `.rot-moe/` beside the existing rules would cover both; the phase shipped the narrower pattern only.</finding>

<finding file="README.md" line="591" severity="medium" confidence="measured">The claims row publishes the instrument's own output as `checker controls: 22 run, 0 failing`. The script prints 25: `checker/counts-sweep.mjs` runs it and parses that very line at counts-sweep.mjs:53, and its summary reads `checker controls 25`; counting the `ok`/`ko` call sites outside the `CC_PLANT_FAIL` branch gives 25 as well, M20, M21 and M22 being this phase's additions. The row's own word "twenty-five" is swept, because the regex at counts-sweep.mjs:165 reads only `([a-z-]+) controls M0 to M(\d+)`; the quoted output string sits past the end of that pattern and no sweep reads it. The evidence column, the one part of the row a reader is invited to reproduce, is the part nothing checks, and its date still reads 2026-09-06.</finding>

<finding file="dtd/sigil/greek-numbers.md" line="61" severity="medium" confidence="measured">The reference table gives 29 as `icosienna-`, and the shipped constructor gives `icosiennea`: `lib/typography.mjs:307` asserts `numeral(29).name === 'icosiennea'` and the control passes while printing that the two disagree. The code discloses the divergence in a comment at lines 303 to 306; the table does not. A reader who consults the document this repository ships as the authority on Greek numerals gets a prefix the corpus never produces, and the passing control means no gate step will ever raise it. Either the table's row or the construction rule is wrong, and nothing in the tree says which.</finding>

<finding file="checker/spdx-sweep.sh" line="10" severity="low" confidence="reasoned">The guard `[ "${#globs[@]}" -ge 1 ] || { echo "spdx-sweep: REUSE.toml declares no annotation path"; exit 2; }` runs under `set -u` (line 14 of the original header, `set -u` at the top). On bash 3.2, which is what `macos-latest` provides and the gate uses it at .github/workflows/gate.yml:34 and :71, expanding an empty array under `set -u` raises "unbound variable" rather than yielding zero. In the one situation the guard exists for, a REUSE.toml with no `path = [...]` line, the macOS leg dies with a shell error and never prints the named refusal or exit 2. The message is unreachable on a third of the matrix.</finding>

<finding file="checker/companion-audit.sh" line="19" severity="low" confidence="reasoned">The usage header documents `exit 2   the verdict entities are not declared, or --score lacks its arguments`. The second half is produced by `${2:?answer file}` and its siblings at line 92, and a bash script that dies on a null-or-unset parameter expansion exits 1, not 2. Only the entity check at line 29 reaches exit 2. A caller branching on 2 to mean "bad invocation" reads a missing argument as an ordinary scoring failure.</finding>

<finding file="checker/companion-audit.sh" line="172" severity="low" confidence="reasoned">The record is staged as `process.argv[2] + ".tmp"`, that is `artifacts/research/companion-<phase>.md.tmp`, and renamed into place. `.gitignore` ignores `artifacts/research/companion-*.json` (line 51) but nothing matching `*.tmp`. If node is killed between the write at line 173 and the rename at line 174, an untracked partial record is left inside a tracked directory, and the next `git add -A` sweeps it in, which is the exact failure the fourteenth pass fixed for the empty-record case.</finding>

### 🩺 Verdict

The instruments this phase built are sound where they were exercised. Every landed proof printed: the slop suite shows its sloppy fixture at 38 hits and 13 tells against a clean fixture at 0, the rotation self-overlap at 1 against a bound of 0.5, the boundary holding at equality; the contract audit closes at 1928 declarations with no unused declaration and no law gap, and its four controls fire; the typography suite runs 34 with every enumeration proved to have a producer; the gate chain reads 83 in gate-sync and in all its published places; counts agree in 61 places and the README index agrees with the resolved tree. Encoding is clean across 864 files, no CR, no BOM, no stray control byte. The seven fixes the sixteenth pass asked for are real: the record now carries its stamp, the law map names 7 and 8 with M20, M21 and M22, the heading capture counts only the four declared words, the REUSE globs are regexes where `*` stops at a slash, and the nested session runs in a temp directory it deletes.

What fails is the same seam as last time, one layer up. The remedy for the tool that hid itself was written as a control scoped to `artifacts/`, and the tool is now hiding at the root, where the control cannot look and the repository's own ignore rule does not reach; the changelog records the leak as closed in a sentence the tree disproves in one command. That is the pattern worth naming: a guard aimed at where the last instance was found rather than at what the instance was an instance of, and a claim written from the intention rather than from a reading. The three lower findings are the same shape at smaller scale, an evidence string nothing re-runs, a reference table nobody holds to the code, and a refusal message the macOS leg cannot print.

### 🩺 Next

Replace `find artifacts -mindepth 2` with a walk from the repository root that names any `.gitignore` not tracked by this repository, and plant the control at the root as well as under `artifacts/`, so the arm is aimed at the class and not at last week's example. Add a bare `.rot-moe/` line to `.gitignore` beside the rules at 40 to 51 and delete the root `.rot-moe/`, then re-read `git check-ignore -v .rot-moe/foo.jsonl` and require the answer to name `.gitignore`, not the foreign file. Correct CHANGELOG.md:543 to say what the tree does. Extend the counts-sweep pattern at counts-sweep.mjs:165 past the word, so the quoted `checker controls: N run` in README.md:591 is re-derived from the run and cannot go stale again, and refresh the row's date. Decide 29 in one place: either fix `dtd/sigil/greek-numbers.md:61` to `icosiennea-` or change the constructor, and turn the disclosure at lib/typography.mjs:307 into a check that goes red while they disagree. Replace `${#globs[@]}` with a `${globs[@]+...}`-guarded form or a plain counter so the no-annotation refusal is printable on bash 3.2. Add `*.tmp` under `artifacts/` to `.gitignore`, or stage the record in the system temp directory instead of beside its target.

COMPANION VERDICT: fail
