<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T11:21:47Z-pid986 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (companion-audit.sh, companion-run.sh, checker-controls.sh in full; enum-sweep, glossary, readme-index, plates, subsets-sweep run through the wrapper), repository root (`SECURITY.md`, `.gitignore` through `git check-ignore -v`), `dtd/` (law-entity census over every `*.dtd`) and `dtd/sigil/` (slop sweep, 5 tracked files), `lib/` (ai-slop, ordinals, encoding engines run; ai-slop controls and ordinals controls both exit 0), `bin/` (adiutor.mjs write sites), `src/skills/dtd-core-dtd/` (slop sweep, 7 tracked files), `artifacts/research/` (tracked list). Both mandated runs pass: `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8` and `ordinals controls: ok (0 failing)`. `lib/encoding.mjs sweep`: 862 files, 0 bad — no CR, no BOM, no missing final LF. `enum-sweep` 0 findings, `glossary`, `readme-index`, `plates`, `subsets-sweep` all in step. Law entities are contiguous and in sequence in all 31 DTDs carrying them. Disclosure under LAW.COMPANION.1: this session's first two Bash calls were issued as `bash …/companion-run.sh node lib/ai-slop.mjs controls < /dev/null; echo "EXIT=$?"` — a redirect and a `;`-chained `echo`, both outside the wrapper's argument check. Nothing was written; the chain is itself the measurement behind the first finding, and every later call was the bare wrapper form.

### 🩺 Findings

<finding file="checker/companion-audit.sh" line="187" severity="high" confidence="measured">The one Bash grant is still a prefix grant: `Bash(bash $here/checker/companion-run.sh:*)` matches anything beginning with the wrapper, and the shell resolves `;`, `&&` and `>` before the wrapper is execed, so the shell-syntax refusal at checker/companion-run.sh:40 never sees them — measured in this audit, `bash …/companion-run.sh node lib/ai-slop.mjs controls < /dev/null; echo "EXIT=$?"` ran the chained `echo` and printed `EXIT=0`, and `> path` in that same position writes a file; this is the exact defect M23 was written to close for `Bash(node lib/ceiling.mjs 60 node:*)`, allow_ok at checker/checker-controls.sh:147 admits the new spelling, and no control plants a chain after the wrapper.</finding>
<finding file="checker/companion-run.sh" line="164" severity="high" confidence="measured">`grep` is in the reading-verb set at line 156 and the flag refusal at line 164 (`--output|--output=*|--set*|--unset*|--add|--replace-all|--edit|-e`) does not carry `-O` or `--open-files-in-pager=`, so `git grep -O<cmd>` makes git execute an arbitrary binary with the matched paths as arguments — measured: `bash checker/companion-run.sh git grep -Ozz-no-such-pager-zz -l LAW.COMPANION.4` answered `error: cannot spawn zz-no-such-pager-zz: No such file or directory` at exit 255, not the wrapper's refusal exit 3, so the spawn was attempted and only the missing name stopped it; a real name there breaks LAW.COMPANION.1 and 2 and defeats "the only binaries the allow-list grants behind that ceiling are node and git".</finding>
<finding file="SECURITY.md" line="25" severity="medium" confidence="reasoned">The security row claims the companion "runs an engine of its table by a reading spelling or a git verb that reads … and refuses everything else by name" and lists M17 to M35 as the verification, but no control covers a shell chain appended to the wrapper's prefix grant or a git reading verb whose own flag spawns a process, so the page states as verified two properties the tree does not hold (the two high findings above).</finding>
<finding file="checker/companion-audit.sh" line="161" severity="medium" confidence="measured">The directory fold `sed 's|/[^/]*$||; s|^[^/]*$|.|'` applies the second expression to the text the first already stripped, so every one-level directory collapses to `.`: the list handed to this run reads `249 .` with no row for `commands/`, `lib/`, `dtd/`, `checker/` or `bin/`, while `git diff --stat v8.0.0..HEAD -- checker/ SECURITY.md dtd/ lib/ bin/` reports 50 changed files in exactly those directories — the folded map tells the companion 249 files changed at the repository root, which is false.</finding>
<finding file="checker/checker-controls.sh" line="226" severity="low" confidence="reasoned">M30's `want30` awk and the wrapper's `--table` sed both key on the same `^      …\.mjs) verbs="…" ;;$` shape, so a row written in any other shape is invisible to both and `n30 -eq want30` still holds; the comment's claim that "the two readers are held to each other" is true only for a miscount, never for a dropped row, and the `-ge 60` floor is the only thing standing between a silently unwalked engine and a green M30.</finding>
<finding file="checker/companion-audit.sh" line="169" severity="low" confidence="measured">The prompt declares "every Bash command you run is 'bash $here/checker/companion-run.sh …'" and in the same sentence orders two runs ending `< /dev/null`, a form that sentence forbids; the wrapper already closes stdin at lines 150 and 167, so the redirect buys nothing and its acceptance is the visible edge of the prefix grant in the first finding.</finding>

### 🩺 Verdict

The build phase is sound where it was measured: every engine control in the walk exits 0, the encoding sweep is clean over 862 files, the law entities are contiguous in all 31 DTDs, the slop gate finds nothing alive-but-dead in the new prose of `dtd/sigil/` and `src/skills/dtd-core-dtd/`, and pass 25's own work holds — `git check-ignore -v` confirms `dist/` (.gitignore:30), `artifacts/cache/` (:52) and `artifacts/research/companion-*.json` (:56) are ignored, so M25's three plants each exercise a different arm of `tree_state` and the runner's own raw stream is the one ignored path filtered back out.

The two high findings are the same failure twice: a boundary asserted in prose and tested only from the inside. Pass 18 removed the prefix grant `Bash(node lib/ceiling.mjs 60 node:*)` because a prefix admits everything after it, and replaced it with `Bash(bash $here/checker/companion-run.sh:*)` — which is a prefix. The wrapper checks its arguments for shell syntax, but the shell has already spent the metacharacters by the time the wrapper is a process; the check can only ever see what survived. The same shape governs the git arm: the verb list decides which git verb runs, and then trusts every flag that verb takes, while `git grep -O` is a documented exec. In both cases the tree comparison would catch a resulting change inside the repository, so this is detected after the fact and not prevented — and a spawn that writes outside the tree, into `~/.claude` or anywhere else, is neither prevented nor detected.

### 🩺 Next

Refuse the chain at the grant, not at the wrapper: the allow-list cannot express "this exact command", so make the wrapper the only thing the string can be — assert in the wrapper that its own `$0` invocation is not a chain is impossible, so instead add a control that plants `bash checker/companion-run.sh git log ; touch zz-chain-control.tmp` through the runner's allow-list and reads whether the file appears, and record the answer either way. Add `-O*` and `--open-files-in-pager*` to the flag refusal at checker/companion-run.sh:164 and trip it in M24 with the same `zz-no-such-pager-zz` probe, asserting exit 3 and no `cannot spawn` line. Fix the fold at checker/companion-audit.sh:161 to keep the top-level directory (`s|^\([^/]*\)$|.|` only where no slash survived the first strip, or fold with awk on the last slash) and assert in a control that a range touching `lib/` yields a `lib` row. Then re-audit: the phase is not closable while the one Bash form is one Bash prefix.

COMPANION VERDICT: fail
