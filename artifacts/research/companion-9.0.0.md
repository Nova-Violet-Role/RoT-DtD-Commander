<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T11:46:23Z-pid685 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (companion-guard.mjs, companion-run.sh, companion-audit.sh, companion-audit.dtd, checker-controls.sh in full), the repository root (SECURITY.md, CHANGELOG.md 740-791), `docs/` (through the slop sweep and `git ls-files docs`), and the changed file list of `dtd/`, `lib/`, `bin/`, `.claude-plugin/` via `git diff --name-only v8.0.0..HEAD`. `src/skills/` and `commands/` were reached only through the sweeps, not opened file by file. Run in this audit: `lib/ai-slop.mjs controls` (0 failing, 117 banned phrases, 8 measures, 8 laws), `lib/ordinals.mjs controls` (0 failing), `lib/encoding.mjs sweep` (6 controls, 863 files, 0 bad — no CR, no BOM, no missing final LF), `checker/enum-sweep.mjs --check` (5619 enumerations, 0 findings), `checker/subsets-sweep.mjs --check` (28 blocks in step), `checker/engines-sweep.mjs` (19 engines, 0 not shipped), `checker/badges.mjs --check` (0 drifted), `git log --oneline -8`, `git ls-files docs`. The PreToolUse hook refused one of my commands: `git grep -c -E "^(ok|\[ ).*ok \"M" checker/checker-controls.sh` was blocked by companion-guard for the `|` in the pattern, `LAW.COMPANION.2`; I did not retry it as Bash and used the Grep tool instead. Every other Bash call was one wrapper call and proceeded. Laws checked in sequence: `LAW.COMPANION.1` to `8` are numbered 1..8 with no gap.

### 🩺 Findings

<finding file="checker/companion-guard.mjs" line="40" severity="high" confidence="measured">judge() reads only tool_input.command, so the Bash tool's own run_in_background parameter is never inspected; a grep of the whole file returns no run_in_background, and the SYNTAX guard at :31 and :46 covers only the shell '&' spelling, so LAW.COMPANION.1's "backgrounds a process" clause has no instrument, and checker-controls.sh:271-276 plants no such payload, so no control can trip on it.</finding>
<finding file="checker/companion-audit.dtd" line="47" severity="medium" confidence="measured">The instrument map for laws 1 and 2 names the allow-list, M17, M23, M24 and the before/after tree comparison and never names checker/companion-guard.mjs or M36, the instrument the twenty-sixth pass added because the allow-list alone was measured insufficient (CHANGELOG.md:766-781, companion-audit.sh:145); the contract's own map of which instrument holds which law is stale for the law it was rewritten to hold.</finding>
<finding file="checker/checker-controls.sh" line="271" severity="medium" confidence="measured">The M36 comment claims a chain, a redirect, a bare command and a writing tool are each "blocked (exit 2 with the reason)", but the assertion at :276 pipes only the chain payload into the hook binary (r36b, exit 2, stderr matched); redirect, bare command and writing tool are exercised only inside judge() by companion-guard.mjs:59-64, which asserts a boolean and never the exit code or the stderr reason the comment claims.</finding>
<finding file="checker/companion-guard.mjs" line="22" severity="low" confidence="measured">The usage comment says "six controls" while controls() has seven say() sites (:55, :56, :58, :60, :62, :64, :66) and prints "7 run" at :67; CHANGELOG.md:780 also says seven, so the file's own comment is the single stale reading.</finding>
<finding file="checker/companion-run.sh" line="33" severity="low" confidence="measured">The --table arm runs a sed and a while-read pipeline before the argument scan at :38 and outside node lib/ceiling.mjs, with no stdin redirect, while LAW.COMPANION.2 declares that every command the companion runs carries its own timeout ceiling and has stdin closed; the arm is reachable from the companion, since "bash .../companion-run.sh --table" is one wrapper call the guard admits, and M30 calls it at :237.</finding>
<finding file="checker/companion-audit.sh" line="119" severity="low" confidence="measured">The ignored-file arm of tree_state drops every path under .rot-moe/, .codemap/ and node_modules/, so a write into those regions of the repository is invisible to the comparison that the DTD names as the instrument for LAW.COMPANION.1, and M25 at :183-188 plants only at the root, under artifacts/cache and under dist/, so no control shows the exclusion cannot hide a breach.</finding>

### 🩺 Verdict

The phase is sound on everything the sweeps can measure: 863 files carry no CR, no BOM and no stray control byte; the slop gate's controls trip on the sloppy fixture and hold on the clean one, including the boundary case where the rhythm equals its bound; the ordinals controls pass; enum-sweep resolves 5619 enumerations with no finding; the subsets, engines and badge checks are in step; the laws of the companion contract are numbered 1 to 8 with no gap; and the head commit message matches what the tree holds. The wrapper's refusals are real and were exercised on me, not only in a control: the hook blocked a pipe inside a git grep pattern and named LAW.COMPANION.2 as the reason.

The one high finding is the newest instrument's blind spot. The twenty-sixth pass established the correct principle — the allow-list grant is a prefix on a shell string, so only a hook that sees the whole call can hold laws 1 and 2 — and then built the hook to read one field of the payload. Backgrounding on this harness is not a shell character; it is a sibling field of the very string the guard inspects, and the guard walks past it. The header comment at :15 lists "background" among the syntax it refuses, which is exactly the conflation that keeps the gap invisible: the word is present, the check is not. Every other prohibition in LAW.COMPANION.1 has an instrument that has been tripped on purpose; this one has none, and a control that was never written cannot go red.

### 🩺 Next

Add a `run_in_background` arm to `judge()` in `checker/companion-guard.mjs` that refuses a Bash payload carrying it, whatever the command string says, and correct the header comment at :15 so "background" names the field and not only the `&` character. Extend M36 in `checker/checker-controls.sh` to pipe four payloads through the hook binary and read exit 2 and the stderr reason for each — the redirect, the bare command, the writing tool and a wrapper call with `run_in_background` set — so the comment at :271 states what the assertion measures. Add `checker/companion-guard.mjs` and M36 to the instrument map at `checker/companion-audit.dtd:47` under laws 1 and 2. Fix "six controls" to seven at `checker/companion-guard.mjs:22`. Then decide whether the `--table` arm should exec under `lib/ceiling.mjs` like the other two arms, and whether M25 should plant a fourth file inside one of the three excluded regions to show what the tree comparison cannot see.

COMPANION VERDICT: fail
