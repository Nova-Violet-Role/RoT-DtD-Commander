<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T11:03:45Z-pid2697 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Read whole: `checker/companion-run.sh`, `checker/companion-audit.sh`, `checker/companion-audit.dtd` (in the prompt), `checker/checker-controls.sh` (1-160, 160-268), `SECURITY.md`, `.gitignore`, `lib/cache.mjs` (header, writer, contract; body through its controls). Folders reached: `checker/`, `lib/`, `dtd/` and `dtd/sigil/` (through the sigil, typography, figure and chain controls, which walk their subsets), `docs/` (plates, glossary, badges, readme-index), `src/skills/` and the built `skills/` mirror (build --check, ai-slop on `src/skills/dtd-core-dtd/references/subsets.md`), `bin/` (build --check), `artifacts/` (check-ignore), repository root. Ran under the wrapper: ai-slop controls (0 failing, 117 banned phrases, 8 measures, 8 laws), ordinals controls (0 failing), cache controls (20/0), chain (26/0), sigil (15/0), typography (35/0), figure (44/0), encoding sweep --tracked (862 files, 0 bad — no CR, no BOM, no missing final LF), enum-sweep --check (5619 enumerations, 0 findings), subsets-sweep --check (28 blocks in step), badges, glossary, plates, readme-index, heading-sweep, gate-sync (83 commands, 0 missing), engines-sweep (19, 0 not shipped), creators-audit, release-notes --versions (9.0.0 everywhere), build --check (303 targets, 0 drifted). Law sequences asserted dense and ascending by their own engines: SIGIL 1-8, FIG 1-8, CACHE 1-8, TYPO, CHAIN. The two runs the prompt names verbatim were refused by the wrapper and were made through it instead; see the third finding.

### 🩺 Findings

<finding file="checker/companion-audit.sh" line="117" severity="high" confidence="measured">tree_state enumerates ignored files under three pathspecs only, artifacts/cache artifacts/sigil artifacts/research, while `git status --porcelain` lists no ignored file at all; `git check-ignore -v` returns .gitignore:46 for .rot-moe/state.json, :30 for dist/x.zip, :13 for commands/planted.md, :16 for skills/planted/SKILL.md, :18 for agents/planted.md, :24 for node_modules/x and :38 for .codemap/x, so a write into any of those seven regions leaves tree_before equal to tree_after and LAW.COMPANION.1 reports no breach — and commands/ and skills/ are the published trees, where a planted file becomes a slash command the operator later runs.</finding>
<finding file="checker/checker-controls.sh" line="184" severity="medium" confidence="measured">M25 plants only at the repository root (zz-tree-control.tmp, seen by the plain status) and under artifacts/cache (inside the pathspec), so both plants land where the guard already looks; nothing is planted under an ignored path outside the three pathspecs, and the control therefore passes on a guard blind to the seven regions above — the alarm cannot trip where the hole is.</finding>
<finding file="checker/companion-audit.sh" line="169" severity="medium" confidence="measured">The prompt orders two runs verbatim, `node $here/lib/ceiling.mjs 60 node $here/lib/ai-slop.mjs controls` and the ordinals twin, in a Bash form the allow-list on line 187 does not grant (it grants only `Bash(bash $here/checker/companion-run.sh:*)`) and that the wrapper refuses even when routed through it: measured exit 3, `companion-run: refused: 60 is not a reading spelling of ceiling.mjs; its reading spellings are: controls`. The same sentence forbids retrying a refused command another way, so the prompt commands a step it also forbids, and the ceiling it names (60) is not the one the wrapper applies (300).</finding>
<finding file="SECURITY.md" line="25" severity="medium" confidence="reasoned">The row states the companion "reads and runs only" is verified by comparing HEAD and the porcelain status before and after the session, with M23 to M35 as the proof; the comparison is blind to the seven ignored regions measured above, so the published claim is wider than the instrument that backs it.</finding>
<finding file="checker/companion-audit.sh" line="117" severity="low" confidence="measured">artifacts/research is passed to `git ls-files --others --ignored --exclude-standard`, but .gitignore:56-57 ignore only companion-*.json and companion-*.md.tmp there; `git check-ignore -v artifacts/research/x.md` matched no rule, so that third pathspec adds nothing beyond the runner's own transcript and reads as coverage it does not give.</finding>

### 🩺 Verdict

Every declared control, sweep, version stamp and encoding check in this range is green, and the two headline additions of the phase — the wrapper `checker/companion-run.sh` and the engine table behind it — hold: node -e, a script outside lib/checker/bin, git commit, git config in its writing forms, a redirect argument and a verb outside an engine's reading set were each refused by name in this audit or by M24 and M28, and the table's own two readers are held to each other by M30. The 1542 new lines of `src/skills/dtd-core-dtd/references/subsets.md` pass the AI_SLOP gate alive (static_share 0.0141, tells 0, closers 0) and their claims are held to `dtd/` by subsets-sweep, 28 blocks in step.

The phase fails on one thing, and it is the thing this phase was built to do. LAW.COMPANION.1 is enforced last by the before-and-after tree comparison, the defence that is meant to catch a write "whatever the allow-list admitted". The twenty-fourth pass widened that comparison to list ignored artifact directories file by file, and listed three. Seven others are ignored at the repository root, two of them the published `commands/` and `skills/` trees, and a write into any of them is invisible to the compare while M25 still reports the guard sound. An incomplete fix that its own control certifies as complete is worse than no fix, because the next pass reads the control line and moves on.

### 🩺 Next

Widen the `ls-files --others --ignored` pathspec in tree_state to the whole work tree — drop the three-directory list and read every ignored untracked file, or read the ignore rules from `.gitignore` rather than naming directories by hand. Then extend M25 with a third plant, under a region outside the current pathspec (`commands/zz-tree-control.md` is the sharpest: ignored by `.gitignore:13`, inside a published tree), and assert the state moves for all three before the control is allowed to pass. Correct `SECURITY.md:25` to say what the comparison actually reads once it is widened. Separately, replace the two verbatim `node lib/ceiling.mjs 60 ...` commands in the prompt at `checker/companion-audit.sh:169` with their wrapper spellings, `bash checker/companion-run.sh node lib/ai-slop.mjs controls` and the ordinals twin, so the prompt stops ordering a command its own allow-list denies.

COMPANION VERDICT: fail
