<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T07:46:16Z-pid1272 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Read in full: `checker/companion-run.sh`, `checker/companion-audit.sh`, `checker/companion-audit.dtd`, `checker/checker-controls.sh`, `checker/spdx-sweep.sh`, `.gitignore`, `dtd/typography.dtd`, `lib/typography.mjs`, `CHANGELOG.md` 9.0.0 tail. Folders reached: `checker/`, `lib/`, `dtd/` (including `dtd/sigil/`), `src/skills/dtd-core-dtd/references/`, repository root.

Ran through the wrapper only: `lib/ai-slop.mjs controls` (0 failing, 117 ban phrases, 8 measures, 8 laws), `lib/ordinals.mjs controls` (0 failing), `lib/typography.mjs controls` (34 run, 0 failing), `lib/encoding.mjs sweep` (6 controls, 862 files, 0 bad — no CR, no BOM, no stray control byte in the range), `checker/controls-sweep.mjs` (33 claims, 0 drifted), `checker/enum-sweep.mjs` (166 sources, 5619 enumerations, 0 findings), `checker/subsets-sweep.mjs`, `checker/counts-sweep.mjs --controls` (checker controls 29 — matches M0..M26 counted by hand), `checker/plates.mjs --controls`, `checker/creators-audit.mjs --controls`, `checker/scala.mjs --controls`, `lib/ai-slop.mjs sweep dtd/sigil` (5 files alive, 0 slop) and `lib/ai-slop.mjs` on `src/skills/dtd-core-dtd/references/subsets.md` (alive, every measure holds). Law numbering swept across `dtd/`: 30 subsets, every `LAW.*` series contiguous from 1, no gap and no repeat.

Disclosure under LAW.COMPANION.1: two of the engines I ran are writers — `checker/spdx-add.mjs` printed `0 added, 426 already present` and `checker/subsets-sweep.mjs` printed `re-embedded 0 of 28`. Neither had anything to write. `git status --porcelain` was empty before and after every command and HEAD is `a188d9f7f5ec9e916bda9efedb5af4c1ecc4a60d` throughout; no write landed. That both were reachable at all is finding 1. `checker/checker-controls.sh` and `checker/spdx-sweep.sh` are bash and the wrapper grants only `node` and `git`, so M23 to M26 and the SPDX sweep were read, not run.

### 🩺 Findings

<finding file="checker/companion-run.sh" line="39" severity="high" confidence="measured">The engine predicate is a directory test and nothing else: any .mjs under lib/, checker/ or bin/ is admitted, and several of those engines write. Measured through the wrapper in this audit: checker/spdx-add.mjs ran and reported "0 added, 426 already present" — it edits tracked files when any lack a header; checker/subsets-sweep.mjs reported "re-embedded 0 of 28" — it rewrites the embedded subsets; checker/seal-secret.mjs printed "usage: node checker/seal-secret.mjs put --name N --file F --nacl DIR [--repo owner/name]" — it publishes a secret to a remote; bin/rot-dtd-commander.mjs printed "install ... default target C:\Users\Saimono\.claude" and "build ... resolve src/ into commands/, skills/, agents/". So LAW.COMPANION.1 is not enforced by the shape, only by the after-the-fact tree comparison at companion-audit.sh:122, and that comparison is blind to three of these: an install writes outside the tree entirely, checker/pack-claude-ai.mjs writes dist/ which .gitignore:30 ignores, and a sealed secret leaves the machine. The wrapper must name the engines it admits, not the folders they sit in.</finding>

<finding file="checker/companion-run.sh" line="53" severity="high" confidence="measured">config is in the list of "reading verbs", and git config is a writer in its plainest form. The flag filter on line 58 names --output, --set, --unset, --add, --replace-all, --edit and -e, and none of those appear in git config core.hooksPath x, git config --global user.name x, or git config --file any/path section.key value — the last is a general-purpose file writer aimed anywhere on disk, and the first is a key git executes. Measured: bash checker/companion-run.sh git config --get core.autocrlf ran to completion and printed "false" at exit 0, so the verb passes the allow-list and the remaining arguments are handed through untouched. M24 at checker-controls.sh:152 tries git commit and stops there, so no control covers this. Drop config from the verb list, or admit only --get, --get-all, --get-regexp and --list.</finding>

<finding file="CHANGELOG.md" line="593" severity="medium" confidence="measured">The release note says the wrapper admits "an engine of this repository under lib/, checker/ or bin/, or a git verb that reads ... refusing by name any other executable, a node flag that evaluates or loads code, a script outside the engines, a git verb that writes and any argument carrying shell syntax", and that a changed tree is read as the breach. The tree contradicts both halves: the engines under those three folders include four writers and one publisher (finding 1), git config writes and is on the reading list (finding 2), and dist/ plus the install target are outside what HEAD and the porcelain status can see. The same claim is repeated as the contract in companion-run.sh:14-19 and in the comment block of checker/companion-audit.dtd.</finding>

<finding file="checker/checker-controls.sh" line="156" severity="medium" confidence="reasoned">M24 asserts [ ! -e "$T/redirect" ] as one of its conjuncts, and that conjunct cannot fail. Line 153 invokes the wrapper as bash $w git log '-1' '>' "$T/redirect" — argv, with no shell between the caller and the wrapper — and companion-run.sh:47 and 61 use exec, so the ">" is a literal argument to git in every branch. Remove the argument filter at line 27 entirely and the file still would not be created; git would merely error on an unknown pathspec. The arm that carries the weight is r24d -eq 2, which is present; the file-absence arm is decoration in a control file whose whole purpose is that every arm can go red.</finding>

<finding file="lib/typography.mjs" line="399" severity="medium" confidence="measured">The producer for the renders enumeration is a typeof test over figure.mjs's exports, so the array it yields is ['cells','svg','both'] whenever figure.mjs still exports renderCells and renderSvg, whatever the plate renderer actually emits. The walk at line 419 therefore reports "renders(3)" — which it did in this run — without any typeset element having been rendered. The neighbouring producers are real (numeral, face, genericOf, classes each build the value they claim); this one can only go red if an export disappears, which the module-resolution error would report first.</finding>

<finding file="dtd/typography.dtd" line="209" severity="medium" confidence="measured">typeset declares classes CDATA #REQUIRED and the comment two lines above says classes "is always rendered", yet no code in the tree emits a typeset element: grep for "typeset" across every .mjs returns only lib/typography.mjs:364-368, where the string is matched inside the resolved subset text by regex. The comment records that the fifth companion pass demoted cols and rows to #IMPLIED for exactly this reason, and left classes on the same footing as the two it demoted. Either a producer emits typeset with its classes attribute, or the attribute joins cols and rows as #IMPLIED.</finding>

<finding file="checker/checker-controls.sh" line="9" severity="low" confidence="measured">The header inventory stops at M19: it lists M1 to M8, M0, "M9 to M16, M18 and M19", and M17 with its two variants, and never mentions M20 through M26 — the run stamp, the heading order, the hand re-score, the old prefix grant, the wrapper's own refusals, the planted tree and the fired ceiling. The file runs 29 controls, which is what checker/counts-sweep.mjs measured in this audit ("checker controls 29") and what CHANGELOG.md:601 states, so the count is right everywhere except in the one comment that claims to enumerate them.</finding>

<finding file="lib/typography.mjs" line="354" severity="low" confidence="measured">The control label reads "the widget face is the viewer own and is never named by us" — the possessive lost its apostrophe, against the entity it is quoting, TYPO.face.widget, which reads "the face is the viewer's and is never named by us". The control passes and prints the broken sentence on every run.</finding>

### 🩺 Verdict

The phase is sound on everything the instruments can measure: 862 files with no CR, no BOM and no stray control byte; every `LAW.*` series in `dtd/` contiguous; 33 changelog control counts undrifted; 5619 enumerations resolved with no finding; the prose added under `dtd/sigil/` and the 1534 lines added to `subsets.md` all alive under the slop gate; and `lib/typography.mjs` shipping 34 controls whose trip arms genuinely trip, including the Greek table now compared row by row rather than sampled.

The failure is in the instrument this phase built to police itself. The eighteenth pass found that a prefix grant is an interpreter and replaced it with a wrapper — but the wrapper checks the *shape* of the invocation and never the *effect*, so it forbids `node -e` while admitting `checker/spdx-add.mjs`, which edits tracked files, and `bin/rot-dtd-commander.mjs install`, which writes outside the tree; and it forbids `git commit` while listing `config` among the reading verbs, where the write form carries no flag at all. Both writes are of the class the tree-state comparison at `companion-audit.sh:122` cannot see, so the second guard does not cover the first guard's hole. The old defect was a grant that admitted an interpreter. The new one is a grant that admits this repository's own writers, and it was documented as closed in `CHANGELOG.md:593`.

### 🩺 Next

Replace the directory test at `companion-run.sh:39` with a named list of read-only engines — `lib/ai-slop.mjs`, `lib/ordinals.mjs`, `lib/typography.mjs`, `lib/encoding.mjs`, `checker/enum-sweep.mjs`, `checker/controls-sweep.mjs`, `checker/counts-sweep.mjs` and the rest that only report — and refuse `spdx-add`, `subsets-sweep`, `pack-claude-ai`, `seal-secret` and `bin/rot-dtd-commander.mjs` by name. Cut `config` from line 53, or narrow it to `--get`, `--get-all`, `--get-regexp` and `--list`. Add M27, which runs `git config --file <tmp> a.b c` through the wrapper and asserts exit 2 with the temp file absent, and M28, which runs `checker/spdx-add.mjs` through it and asserts exit 2 — both with the landed proof printed before the exit code is read. Then correct `CHANGELOG.md:593`, `companion-run.sh:14-19` and the instrument map in `companion-audit.dtd` to describe what the wrapper refuses after the narrowing, and drop the vacuous `[ ! -e "$T/redirect" ]` conjunct from M24.

COMPANION VERDICT: fail
