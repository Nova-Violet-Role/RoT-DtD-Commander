<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Twelve commits, 337 files, all present and encoding-clean (0 BOM, 0 CR, 0 control bytes outside TAB/LF, measured over every path in the range). Both mandated instruments run green: `lib/ai-slop.mjs controls` reports `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8`, `lib/ordinals.mjs controls` reports `ordinals controls: ok (0 failing)`. Also run in the foreground under the ceiling with stdin closed: `checker/controls-sweep.mjs` (15 claims, 0 drifted), `checker/counts-sweep.mjs` (139/22/5, 40 places in step), `checker/contract-audit.mjs` (1928 declarations, 0 unused, 0 law gaps — the law-sequence scan I ran myself flagged only continuation prefixes and it is right), `checker/gate-sync.mjs` (73, 0 missing), `lib/ai-slop.mjs sweep dtd/sigil artifacts/research --max 0` (22 files, 0 slop), and the AI_SLOP gate on each of the three new commands (all `alive=yes`, tells 0, closers 0). Read: `dtd/cc-sigil.dtd`, `dtd/typography.dtd`, `lib/sigil.mjs`, `lib/typography.mjs`, `checker/controls-sweep.mjs`, the three new command sources and the built copy of `commands/typography-dtd.md`.

Disclosure, LAW.COMPANION.1: I ran `node checker/readme-index.mjs` without a flag and it is a writer — it rewrote the README block and two map plates. `git status --porcelain` after the call shows only `artifacts/research/companion-9.0.0.json`, the runner's own record, so the write was byte-identical and the tree is unchanged, but the call was a breach of the read-only bound and is recorded here rather than omitted.

### 🩺 Findings

<finding file="lib/sigil.mjs" line="299" severity="high" confidence="measured">The trial census is a tautology and no instrument holds TRIALS to the forms the subset declares: `controls()` calls `run('all', c)`, and at scope `all` the filter on line 238 returns TRIALS unchanged, so `r.trials.length === TRIALS.length` is true by construction and can never trip. Measured consequence: tiers S, A and B declare 42 forms with context `shell`, TRIALS holds 41 entries of which one is the tier D `deprecated-arithmetic`, so 40 of 42 run; `process-substitution-out` and `locale-quote` are declared in `dtd/cc-sigil.dtd` and have no trial, no comment excusing them, and no control that names the gap. The run reports `0 fail` while two declared forms were never executed, which is the shape LAW.SIGIL.7 exists to forbid.</finding>

<finding file="src/commands/typography-dtd.md" line="89" severity="high" confidence="measured">The command's own output template contradicts the grammar it includes. `dtd/typography.dtd:192` declares `form (unit|teen|ten|compound|scale|plain)`, LAW.TYPO.7 at `dtd/typography.dtd:213` requires the three exact scales to keep their names under form `scale`, and `lib/typography.mjs:196` produces it (`numeral(100).form === 'scale'`, asserted at `lib/typography.mjs:270`). The template renders `form [unit|teen|ten|compound|plain]` — five of six values, `scale` missing — so a run asked to name a 100-sided figure has no declared cell to put the engine's answer in. The built copy carries the same line at `commands/typography-dtd.md:1026`. This is drift the fourth pass introduced and did not finish: the carve-out reached the DTD, the engine and the control, and not the command it is rendered by. Nothing can catch it — rule C15 in `lib/dtd.mjs:295` walks elements against the grammar_map and no checker walks an ATTLIST enumeration against the output template, so the producer walk at `lib/typography.mjs:380` passes on the DTD side while the command stays wrong.</finding>

<finding file="src/commands/verbs-dtd.md" line="41" severity="medium" confidence="measured">The objective claims the command "executes the shell forms of SIGIL.tier.S, SIGIL.tier.A, SIGIL.tier.B and the one tier D form that may be shown", and the front-matter description on line 2 promises "every form of the sigil run together". Measured against the engine: 42 shell-context forms in those three tiers, 40 executed. The claim is a census the tree does not support, and because of the finding above nothing will report it when the gap widens.</finding>

<finding file="checker/controls-sweep.mjs" line="35" severity="medium" confidence="measured">`\Z` is not an end-of-input anchor in JavaScript; it is an identity escape for the literal character `Z`. Measured directly: `/\Z/.test('Z')` is `true` and `/\Z/.test('Q')` is `false`, and `newestSection` returns `null`/`''` for a changelog whose newest section is the last one and contains no capital Z. Today the section is followed by `## 8.0.0` and the `m[0] + rest.slice(0, next.index)` reconstruction hides it, so the output is correct by accident. On a one-section changelog the gate arm at line 154 reads zero claims, `found.length !== loose` is `0 !== 0`, the refusal at line 89 never fires, and `--check` exits 0 reporting `0 claims, 0 drifted` — a green from a sweep that read nothing, the same class of defect the third pass rated high in this same file. The `--controls` arm's `found.length >= 3` floor at line 117 catches it in the full gate chain, which is why this is medium and not high; the `--check` step has no floor of its own.</finding>

<finding file="src/commands/typography-dtd.md" line="90" severity="medium" confidence="reasoned">`dtd/typography.dtd:200` declares `cols`, `rows` and `classes` as `#REQUIRED` on `typeset`, and the `### 🔤 Typeset` template renders none of the three — its closing line carries the cell, the baseline, the control count and the declarations spent instead. A required attribute with no place in the rendering cannot be checked against the answer, and for a run invoked as "the contract alone" there is no grid for `cols` and `rows` to describe, so either the template owes three values or the attributes owe a `#IMPLIED`.</finding>

### 🩺 Verdict

The phase is sound in the places it built instruments for and thin in the place it did not: the enumeration-to-producer walk, the controls sweep and the counts sweep all hold, the encoding is clean across all 337 files, the law prefixes are dense and ascending, and every prose file added in the range survives the recognizer. The two high findings are the same defect wearing two coats — a declaration that reaches the DTD and the engine but not the artifact that renders it, with no instrument standing between them. `lib/sigil.mjs:299` is worse than a missing check because it reads as a census and is arithmetic on a value that cannot differ; it is the exact failure the tree's own no-stall discipline names, an alarm nobody can trip. The `scale` omission is the fourth pass's carve-out landing three places out of four, and the gate is structurally blind to the fourth. Both are cheap to close and neither is a design error.

### 🩺 Next

Add `scale` to the form enumeration in `src/commands/typography-dtd.md:89` and rebuild, then add a checker that walks every ATTLIST enumeration of an included subset against the enumerations spelled in the including command's `output_format`, tripped on a planted omission — without it this exact drift recurs on the next carve-out. Replace `r.trials.length === TRIALS.length` at `lib/sigil.mjs:299` with a census that holds TRIALS to the shell-context forms of tiers S, A and B and names any form with no trial, then either add trials for `process-substitution-out` and `locale-quote` or declare in the engine why they are exempt, and correct the claim at `src/commands/verbs-dtd.md:41` to the number the census prints. Fix `\Z` in `checker/controls-sweep.mjs:35` to `$` with the `m` flag removed for that alternative, and give the `--check` arm the same claim floor the `--controls` arm has. Settle `cols`, `rows` and `classes` on `typeset` in one direction or the other.

COMPANION VERDICT: fail
