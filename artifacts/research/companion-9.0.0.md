<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T10:18:43Z-pid1024 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: repository root (`README.md`, `SECURITY.md`, `CHANGELOG.md`, `CITATION.cff`, `package.json` via counts-sweep), `checker/` (`companion-run.sh`, `companion-audit.sh`, `companion-audit.dtd`, `checker-controls.sh`, `counts-sweep.mjs`), `dtd/` including `dtd/sigil/`, `lib/` through engine runs, `src/commands/`, `src/skills/`, `src/agents/`, `agents/`, `commands/`, `.github/workflows/`, `docs/` through grep.

Run in this audit, all through `checker/companion-run.sh`: `lib/ai-slop.mjs controls` (slop controls ok, 0 failing, ban list 117 phrases, 8 measures, 8 laws), `lib/ordinals.mjs controls` (ok, 0 failing), `lib/encoding.mjs sweep dtd/sigil` (6 controls, crlf-sweep 862 files checked, 0 bad — no CR, no BOM, no missing final LF), `lib/ai-slop.mjs sweep --tracked dtd/sigil` (5 files, 0 slop, all alive), `lib/typography.mjs controls` (35 run, 0 failing), `lib/cache.mjs controls` (20 passed, 0 failed), `lib/sigil.mjs controls` (15 run, 0 failing), `checker/subsets-sweep.mjs --check` (28 blocks in step), `bin/rot-dtd-commander.mjs check` (`checked 166  failed 0`), and reading `git` verbs.

Numbers re-derived and found in step: `dtd/*.dtd` = 31, matching the README doctor row `31 subsets, every one installed`; `^## [a-z-]+\.dtd$` in `src/skills/dtd-core-dtd/references/subsets.md` = 28, matching `SKILL.md:90` "the 28 grammars quoted verbatim"; `dtd/cc-*.dtd` = 19, matching `agents/dtd-contract-auditor.md:3` "nineteen of them"; `git grep -l -F "EUPL-1.2)" -- src` = 156 files, 24 commands, 10 skill directories, matching `README.md:722` and `README.md:705`; `ok "` calls in `checker-controls.sh` = 35 outside the `CC_PLANT_FAIL` branch, matching `README.md:591` "thirty-five controls M0 to M32" and `35 - 7 - 2 = 26` in the gate step name and the verify comment; `checkerSpan` = 32, matching `SECURITY.md:25` "M23 to M32"; the wrapper ceiling read from `companion-run.sh:150` = 300, matching the security page. `all tripped as designed` is printed by `checker-controls.sh:241`, not merely quoted. LAW entities in `typography.dtd`, `geometry.dtd`, `cc-sigil.dtd`, `cc-figure.dtd`, `cc-chain.dtd`, `cc-cache.dtd`, `codebase-generator.dtd` and `companion-audit.dtd` are all dense and ascending from 1.

The tree was clean before and after this audit: `git status --porcelain` empty, and `git status --porcelain --ignored artifacts/cache` empty. No write reached the tree by any path.

### 🩺 Findings

<finding file="checker/checker-controls.sh" line="103" severity="high" confidence="reasoned">M9 is a control that cannot trip. `score()` in checker/companion-audit.sh:54-94 returns 0 only through the `last = "$vpass"` branch, so for any answer whose last non-empty line is `COMPANION VERDICT: fail` it returns 1 unconditionally — through LAW.COMPANION.4, .3, .6, .7 or .8, or through the ordinary FAIL path. M9's plant ends in `COMPANION VERDICT: fail` and M9 asserts only `[ $rc -eq 1 ]`, with no grep on the message, unlike M10, M11, M14, M15 (which grep `a fail with no high finding`), M16 (`LAW.COMPANION.3`), M18/M19 (verdict-line counts) and M13 (`LAW.COMPANION.6`). Worse, the exit 1 M9 observes today is not the one it names: its plant carries only `### 🩺 Findings`, so `heads` reads `Findings ` and the scorer returns at checker/companion-audit.sh:91 under LAW.COMPANION.8, never reaching a verdict on the high finding at all. Delete the `nhigh` counter at checker/companion-audit.sh:60 entirely and M9 still prints PASS, while the suite total stays 35 and README.md:591 keeps publishing `all tripped as designed`. The one control that claims to prove a high finding in the element spelling scores as a fail proves nothing.</finding>

<finding file="checker/companion-run.sh" line="87" severity="medium" confidence="measured">The wrapper's own header (checker/companion-run.sh:63-69) says an engine is admitted by what it does and that writers are refused by name, and LAW.COMPANION.1 says the companion never writes. The table grants `lib/cache.mjs controls`, which writes. Run in this audit, it printed `save writes the file under CACHE.dir, reads it back byte-equal ... (579 bytes)`, `consume deletes the file`, plants `artifacts/cache/short-dtd.nt` and `artifacts/cache/other-dtd.nt`, and reports `git check-ignore artifacts/cache/probe-58336.nt: exit 0`. The guard that holds LAW.COMPANION.1 is the `tree_state` comparison at checker/companion-audit.sh:106-114, which reads `git status --porcelain` — blind to every path under `.gitignore` by construction, so LAW.CACHE.6 (the property that keeps a cache out of a commit) is exactly what hides these writes from the guard. M25 plants `zz-tree-control.tmp` at the repository root, a tracked-visible path, so it proves only the half of the guard that already works. Nothing today holds `lib/cache.mjs controls` to cleaning up after itself; I measured that it does (`git status --porcelain --ignored artifacts/cache` empty after the run), which is a fact about this revision, not a control.</finding>

<finding file="checker/companion-run.sh" line="32" severity="low" confidence="measured">The `--table` arm returns at checker/companion-run.sh:34, before the shell-syntax scan of the argument list at line 38-42, before any `< /dev/null`, and before the `exec node lib/ceiling.mjs 300` at line 150 or 167. LAW.COMPANION.2 declares that every command the companion runs carries its own timeout ceiling and has stdin closed; the allow-list grant `Bash(bash $here/checker/companion-run.sh:*)` at checker/companion-audit.sh:176 is a prefix, so `bash checker/companion-run.sh --table` is admitted to the companion as an unbounded command with stdin inherited. The `sed` reads `$0`, a fixed local file, so it terminates in practice — the law is broken in form, not yet in consequence.</finding>

<finding file="checker/companion-audit.sh" line="59" severity="low" confidence="reasoned">The scorer counts `nfind` as lines beginning `<finding ` and requires every one to carry all four attributes in its opening tag. M15 proves that `severity="high"` quoted inside a finding body counts for nothing, but no control plants the whole element spelling in a body. A companion that quotes `<finding file=...` at column zero while describing this very grammar — the natural thing to do when the audit target is the contract itself — adds a phantom element to `nfind`; if the quoted line lacks an attribute, LAW.COMPANION.3 fires at checker/companion-audit.sh:65 and an otherwise sound pass is refused. The asymmetry is that the attribute is protected from quoting and the element is not.</finding>

### 🩺 Verdict

The phase is sound on the numbers, the encoding and the prose. Every count I re-derived independently agrees with what the tree publishes: 31 subsets, 28 quoted grammars, 19 shared subsets, 156 MIT-converted sources, 24 commands and 10 skills, 35 controls spanning M0 to M32, a wrapper ceiling of 300, `checked 166  failed 0`. The 862-file encoding sweep found no CR, no BOM and no missing final LF; the AI_SLOP gate passed all five new `dtd/sigil/` documents alive with zero hits; the LAW entities of all seven new or changed DTDs are dense and ascending. The wrapper is a real improvement over the prefix grant it replaced, and M24, M27, M28 and M29 trip its refusals by name with measured exit codes.

The phase fails on one thing, and it is the thing this pass exists to find. M9 cannot trip. It is the only companion-scorer control in the suite that asserts an exit code without asserting the message that explains it, and because `score()` cannot return anything but 1 for an answer ending in a fail verdict, its assertion is satisfied by a scorer with the high-finding counter removed. Right now it is not even failing for its own reason: LAW.COMPANION.8, added this phase, short-circuits it at checker/companion-audit.sh:91 because its plant was never given the four headings that M12's plant was given in the same commit. Eight sibling plants were left carrying only `### 🩺 Findings`; seven of them survive because the law they name fires before the headings check, and M9 is the one that does not. A suite that publishes `all tripped as designed` over a control that cannot fire is the exact defect the suite was built to refuse, and the checker's own total of 35 counts it as live.

The two lower findings are the same shape one order down: a guard whose blind spot is a granted engine's working directory, and a law honoured everywhere except the arm that returns before the ceiling is applied.

### 🩺 Next

Give M9 the message assertion its siblings have — grep for the FAIL path, not merely exit 1 — and give its plant the four headings, so the control fails for the reason it names. Then prove it: gut `nhigh` at checker/companion-audit.sh:60 and confirm M9 goes red, because a control that has never been seen red is a control nobody has tested. Audit the other seven fail-verdict plants (M10, M11, M14, M15, M16, M18, M19) the same way, confirming for each that the law it names fires before LAW.COMPANION.8 and not after. Extend M25 to plant a file under an ignored path as well as a tracked one, so the tree-state guard's blind spot is named in a control rather than discovered by a companion; or refuse `lib/cache.mjs controls` in the table alongside the other writers. Move the `--table` arm below the argument scan, or state in the wrapper header that it is the one form exempt from LAW.COMPANION.2 and why.

COMPANION VERDICT: fail
