<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T12:10:14Z-pid1998 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Folders read: `checker/` (`companion-run.sh`, `companion-guard.mjs`, `companion-audit.sh`, `companion-audit.dtd`, `checker-controls.sh`, `counts-sweep.mjs` line by line), repository root (`SECURITY.md`, `CHANGELOG.md`), `dtd/` (the law entities of the six changed subsets `cc-cache`, `cc-chain`, `cc-sigil`, `cc-figure`, `typography`, `geometry` — all numbered 1..n in sequence), `docs/`, `artifacts/research/`. `lib/` was reached by running its engines, not by reading each file. `src/commands`, `commands`, `src/skills`, `skills` were reached through the engines that walk them, not file by file: `enum-sweep --check` (166 sources, 5619 enumerations, 0 findings), `frontmatter-sweep --check` (166 parse, 0 would change), `readme-index --check` and `glossary --check` (139 commands, 22 skills, 5 agents, in step), `subsets-sweep --check` (28 blocks in step).

Both commanded controls were run: `lib/ai-slop.mjs controls` printed `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8` with every landed proof, and `lib/ordinals.mjs controls` printed `ordinals controls: ok (0 failing)`. `lib/encoding.mjs sweep checker` printed `crlf-sweep: 863 files checked, 0 bad` — no CR, no BOM anywhere. `SECURITY.md` and `artifacts/research/companion-9.0.0.md` both pass the AI_SLOP gate (`alive=yes`, every measure holds).

The hook refused one of my commands: `companion-run.sh git grep -n "M36\|M25\|..."` was blocked by `companion-guard.mjs` with `a wrapper call carries no chain, pipe, background, redirect, substitution, backtick or newline` — the alternation bar in the pattern is read as shell syntax. I did not retry it another way; I used the Grep tool. The wrapper itself refused `git log -p` by name. No other refusal, and I wrote nothing.

### 🩺 Findings

<finding file="checker/companion-run.sh" line="170" severity="high" confidence="measured">The git arm runs `-c diff.external=`, which does not disable an external diff but sets it to the empty command, so every patch-producing `git diff` dies: `companion-run.sh git diff v8.0.0..HEAD -- CHANGELOG.md` exits 128 with `error: cannot spawn : No such file or directory` / `fatal: external diff died`, while `git show HEAD -- CHANGELOG.md` on the identical path exits 0 with its patch (git honours diff.external in the diff porcelain and ignores it in show without --ext-diff), and `git diff --no-ext-diff v8.0.0..HEAD -- checker/companion-audit.dtd` exits 0 with the patch — so the verb `diff`, granted by name at line 157 and published as "a git verb that reads" in SECURITY.md:25, cannot read the git range this whole instrument exists to audit; the fix is `--no-ext-diff` in place of `-c diff.external=`.</finding>
<finding file="checker/checker-controls.sh" line="181" severity="high" confidence="measured">No control anywhere exercises a patch-producing git verb, so the wrapper's dead `git diff` is an alarm that cannot trip: M24 runs only `git commit`, `git log -1`, `git rev-parse HEAD` and `git grep -O` (lines 174-180), M27 runs three `git config` forms (lines 199-201), and the M30 table walk at lines 232-239 loops `bash $w node "$e" $args` over node engines alone — the git arm has no table and no walk, which is why exit 128 on the wrapper's headline reading verb survived twenty-seven passes green.</finding>
<finding file="checker/companion-run.sh" line="166" severity="medium" confidence="measured">The flag scan refuses `-p` with the reason `opens a pager, an editor or an external diff, which runs a command (LAW.COMPANION.1)`, measured on `companion-run.sh git log -p -1` (exit 3), but after a verb `-p` is `--patch` and never a pager — `--paginate` is `git -p` before the verb, which this arm already fixes with `--no-pager`; the refusal reason is false and, combined with the finding above, it leaves the companion with no patch view through any granted verb but `git show`. `--no-textconv` on the same line is refused for the same wrong reason, though it is the spelling that disables textconv.</finding>
<finding file="SECURITY.md" line="25" severity="medium" confidence="measured">The security page enumerates the M25 plants as "a changed tree, at the root, under an ignored artifact directory or under dist/" — three — where M25 at checker-controls.sh:183-190 now plants four and asserts all four, the fourth being `.codemap/zz-tree-control.txt` under the index cache; the commit that added the fourth plant (263c2a1, "M25 plants a fourth file under the index cache") touched SECURITY.md in the same change and left this row at three.</finding>
<finding file="checker/companion-audit.dtd" line="52" severity="low" confidence="measured">The law map says "M36 pipes five payloads through it" where M36 pipes six: checker-controls.sh:277-282 calls `hook` six times (chain, redirect, bare command, Write, run_in_background, and one plain wrapper call read at `r36c`), five blocked and one admitted. This is the identical undercount the twenty-seventh pass itself repaired ("M36 piped one payload where its comment claimed four", CHANGELOG.md:798).</finding>
<finding file="CHANGELOG.md" line="805" severity="low" confidence="measured">The twenty-seventh pass entry carries the same undercount as the law map, "M36 pipes five payloads through the binary and reads exit 2 and the reason for each", and so does the commit subject of 263c2a1 ("five payloads through the hook"); six payloads are piped and only five are read at exit 2, so the sentence is true of the blocked set and wrong about what M36 pipes.</finding>

### 🩺 Verdict

The phase is sound in everything the instruments measure: encoding is clean across 863 files, both commanded control suites are green with their landed proofs printed, the six new and changed DTD subsets number their laws 1..n without a gap, the five reading sweeps over `src/`, `commands/`, `skills/` and `dtd/` are in step, and the two prose files most at risk pass the AI_SLOP gate alive. The hook added by the last two passes is live and refused one of my own commands correctly.

The fail is for the git arm of the wrapper. `-c diff.external=` was added to close an external-diff hole and instead opened a worse one: it installs an empty external diff command, and every `git diff` that would print a hunk exits 128 before printing a byte. The companion is told, in the prompt this session was launched with, to audit a git range; the one verb that shows a range's content has been dead for the whole life of the wrapper, and `-p` is refused on top of it, so `git show` is the only surviving patch view and only by the accident that show ignores diff.external. The instrument that should have caught this does not exist: the git arm is the only arm of the wrapper with no table and no walk, and its four M24 spot checks all happen to be verbs that print no patch. A green suite over a verb that cannot run is the precise failure the contract's own laws 3 and 5 were written against, so it is reported at high rather than as an inconvenience.

### 🩺 Next

Replace `-c diff.external=` with `--no-ext-diff` at companion-run.sh:170 and confirm with the landed proof already measured here: `git diff v8.0.0..HEAD -- CHANGELOG.md` must go from exit 128 to exit 0 with a patch. Drop `-p` and `--no-textconv` from the line 166 refusal list, or move them to a list whose reason names `--patch` correctly. Then give the git arm the walk the node arm has: a table of the granted verbs with a reading spelling for each, walked by a new control that fails a verb which exits 3, 2, 124, or prints a `fatal:` line — the walk is what makes the fix hold, not the fix. Correct SECURITY.md:25 to four plants, and correct "five payloads" to six in companion-audit.dtd and CHANGELOG.md:805.

COMPANION VERDICT: fail
