<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Sixth pass. Fifteen commits, 340 files. Both mandated instruments run green under the ceiling with stdin closed: `lib/ai-slop.mjs controls` prints `slop controls: ok (0 failing), ban list 117 phrases, measures 8, laws 8`; `lib/ordinals.mjs controls` prints `ordinals controls: ok (0 failing)`. Also run, all green: `checker/counts-sweep.mjs --controls` (6 run, 0 failing; 139/22/5, 40 places in step), `checker/controls-sweep.mjs --check` (18 claims, 0 drifted), `checker/enum-sweep.mjs --check` (166 sources, 5619 enumerations, 430 spelled choices, 0 findings), `checker/contract-audit.mjs` (1928 declarations, 0 unused, 0 law gaps), `checker/gate-sync.mjs` (75, 0 missing), `checker/heading-sweep.mjs --check`, `checker/pack-claude-ai.mjs --controls` (27 run, 0 failing), `lib/sigil.mjs controls` (15 run), `lib/sigil.mjs run` (pass 43), `lib/typography.mjs controls` (32 run), `lib/ai-slop.mjs` on README, CHANGELOG, RELEASE, NOTICE (exit 0 each, read directly, not through a pipe) and `sweep dtd/sigil artifacts/research --max 0` (22 files, 0 slop). Encoding census over all 895 non-git files in the tree: 0 BOM, 0 CR, 0 stray control bytes in any tracked file; the 18 CR files are `.cocoindex_code/` and `.codemap/` tool state, untracked and outside the repository's own bytes.

The three fifth-pass repairs land: the sigil census now holds the tiers to the trials by name in both directions (`lib/sigil.mjs:308`), `newestSection` slices by index instead of leaning on `\Z`, and `cols`/`rows` on `typeset` are `#IMPLIED`. What follows is new.

### 🩺 Findings

<finding file="lib/typography.mjs" line="155" severity="high" confidence="measured">The both-directions enumeration walk is blind to one of the subset's seven enumerations, and the control's pinned count freezes the blindness. `enumerations()` reads values with the character class `[a-z_|]+`, which cannot match a hyphen, so `generic (monospace|serif|sans-serif)` at `dtd/typography.dtd:93` never enters the walk. Measured: the same file scanned with `[^)]+` yields 8 matches including `generic`, the shipped regex yields 6, and the control's own output line names exactly `role(2) style(2) class(6) form(6) suffix(4) renders(3)` while claiming "every value of every ATTLIST enumeration has a producer and nothing undeclared is produced". Two declared values, `serif` and `sans-serif`, have no producer anywhere — `face()` at line 165 returns `monospace` for `plate` and `''` for `widget`. Worse, `lib/typography.mjs:321` retypes the enumeration by hand as `const GENERICS = ['monospace', 'serif', 'sans-serif']` in a module whose header says "nothing about the repertoire, the cell, the classes or the numeral series is repeated here, so the file and the code cannot drift without a control seeing it". Landed proof that the guard cannot trip: applied to `generic (monospace|serif|sans-serif|cursive)`, the shipped regex returns zero matches for the whole line, so a fourth value leaves `enums.length === 6` true at line 380 and `gaps` empty, and the suite stays green. This is the fifth pass's own defect one file over — a declaration that reaches the subset and nothing that renders or produces it.</finding>

<finding file="CHANGELOG.md" line="81" severity="high" confidence="measured">The release-day measurement block carries a count the tree contradicts. Line 81 reads ``- `node lib/sigil.mjs controls`: 15 run, 0 failing; `node lib/sigil.mjs run` on windows, bash 5.3: 41 pass, 0 fail, 0 unsupported``. Run here on windows, bash 5.3, the command's last line is `tally: pass 43, fail 0, unsupported 0`. The section opens at line 24 with "Every count below was produced by the instrument named beside it on the release day", and the same file contradicts itself at line 215, where the fifth-pass section says "the run executes 43". The instrument built to catch exactly this class cannot see it: `checker/controls-sweep.mjs:52` matches only ``` `node <path> (--controls|controls)`: N run|passed ```, so a `run` claim on the same line is outside its reader, and `looseCount` at line 66 counts only the same two verbs, so the refusal-by-number at line 93 does not fire either. The fifth pass added `process-substitution-out` and `locale-quote` and updated its own prose and not the release block.</finding>

<finding file="src/commands/starlist-dtd.md" line="122" severity="high" confidence="measured">Every grammar_map in the tree carries the generated sentence "One declared element per heading, in declared order" (`lib/dtd.mjs:372`), and nothing checks the order. Measured on the 139 sources under `src/commands`, comparing the root's content model to the heading sequence of the template through the grammar_map rows: 13 render out of declared order. The clean case is this file. `starlist_run` is declared `(args, probe, substrates, measured, intake, tools, bounds, verdicts, refused*, next_action, assumption_made*)` at line 39, the grammar_map rows list `measured` at line 97 before `intake` at line 98, and the template renders `### ⭐ Intake` at line 122 before `### ⭐ Measured` at line 129 — the reverse of both, and the reverse of LAW.STAR.4's own words, "what the walk found before the first question". The other twelve hoist `assumption_made` from last to third (`chain-dtd`, `sigil-dtd`, `typography-dtd`, `verbs-dtd`, the four codebase-* commands, the three amplify-band commands, `deep-scratch-dtd`). No instrument stands here: rule C15 at `lib/dtd.mjs:295` asks only whether an element is "rendered by no grammar_map row", never in what position; `checker/heading-sweep.mjs` reshapes bold lines into headings and its only mention of "declared order" is inside a planted fixture string at line 62; `checker/enum-sweep.mjs` walks enumeration values, not sequence.</finding>

<finding file="artifacts/amplify-codebase/state.md" line="101" severity="medium" confidence="measured">The same stale sigil tally, spelled in words, in a ledger row marked `done`: "node lib/sigil.mjs run executes forty-one forms per leg and records pass, fail or unsupported with the bash it needs". The engine executes 43 trials, 42 shell forms of tiers S, A and B plus the one tier D form shown under its rule, which `lib/sigil.mjs:313` now prints by name. `checker/counts-sweep.mjs` reads counts in words but its 40 places are README, the manifests and the package description; this file is not among them, so the row will stay wrong silently.</finding>

<finding file="lib/chain.mjs" line="359" severity="low" confidence="measured">The comment reads "The two enumerations the DTD declares are the two the code produces, in both directions". `dtd/cc-chain.dtd` declares ten ATTLIST enumerations — `autonomy`, `gate`, `trust`, `asked`, `runs_alone`, `takes`, `ran`, `why`, `as`, and a second `trust` — of which the two controls below hold `why` and `takes`. The sentence states a census the file contradicts; `ran (yes|no|refused)`, `runs_alone (yes|no)` and `autonomy (gated|no-gate)` are produced by this module and walked by nothing. This is the shape the first pass already corrected once in this file, in the `runs_alone` comment.</finding>

### 🩺 Verdict

The instruments built in the last five passes hold: the controls sweep re-reads its own changelog section and refuses an unreadable claim, the enumeration sweep walks 5,619 declarations against 430 spelled choices and finds nothing, the sigil census names its forms instead of counting an array against itself, the counts sweep keeps 40 places in step, and every prose file in the range survives the recognizer. What this pass found is that each new instrument was drawn tightly around the defect that produced it and left the adjacent inch uncovered. The enumeration walk in `lib/typography.mjs` was built to hold values to producers and its character class silently drops the one enumeration whose values carry a hyphen, with a pinned count that makes the omission permanent. The controls sweep was built to re-run every measured count in the release block and reads one of the two verbs on its own line 81, so the sigil tally the fifth pass changed went stale in the block that promises it did not. The enum sweep was built because no checker walked a declaration against the template that renders it, and no checker walks the declared *order* against that same template either — thirteen of a hundred and thirty-nine commands render against the sentence their own grammar_map states. Three findings, three instruments, one defect: the guard was drawn at the width of the last failure rather than the width of the law.

### 🩺 Next

Read `generic` into the walk: widen `enumerations()` at `lib/typography.mjs:155` to `([\w|.:\s-]+?)` — the class `checker/enum-sweep.mjs:59` already uses — replace the literal `GENERICS` at line 321 with `enumeration('generic')`, give `serif` and `sans-serif` a producer through a third declared face or drop them from the subset, and change `enums.length === 6` at line 380 to a count read from the file so a new enumeration cannot be added invisibly; trip it by planting a fourth value and confirming the suite goes red before trusting the green. Correct `CHANGELOG.md:81` to 43 and `artifacts/amplify-codebase/state.md:101` to forty-three, then widen the claim regex in `checker/controls-sweep.mjs:52` and `looseCount` at line 66 to read `run` and its `N pass, N fail, N unsupported` tail, so the next tally that moves is re-run rather than reprinted. Add an order arm to `checker/enum-sweep.mjs` or a new sweep: for each source, map the template's headings back through the grammar_map rows and refuse a sequence that inverts the root's content model, tripped on a planted transposition; then fix `src/commands/starlist-dtd.md` by moving `### ⭐ Measured` above `### ⭐ Intake`, and settle the twelve `assumption_made` hoists in one direction — either move the heading last or declare the element earlier in each content model. Correct the census sentence at `lib/chain.mjs:359` to name the two enumerations it walks and the eight it does not.

COMPANION VERDICT: fail
