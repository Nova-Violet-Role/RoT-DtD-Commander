<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->
<!-- companion run: 2026-09-07T12:26:53Z-pid2655 -->

### 🩺 Scope

phase=9.0.0 range=v8.0.0..HEAD model=opus

Read: `checker/` (companion-run.sh whole, companion-guard.mjs whole, companion-audit.sh whole, checker-controls.sh lines 1-100 and 130-292, counts-sweep.mjs around line 105), `checker/companion-audit.dtd` (diff and whole), `SECURITY.md` (diff), `artifacts/research/companion-9.0.0.md` (headings), plus the diff stat over `checker/`, `dtd/`, `lib/`, `bin/`, `SECURITY.md`. Ran through the wrapper: `lib/ai-slop.mjs controls` (8 laws, 0 failing), `lib/ordinals.mjs controls` (0 failing), `lib/encoding.mjs sweep checker` (863 files, 0 bad — no CR, no BOM), `lib/ai-slop.mjs SECURITY.md` (alive=yes, every measure holds), `--table` (67 rows), `git log -3 --oneline`, `git diff-tree -r HEAD`, `git diff` twice. `git log` and `git diff-tree` are untested by M24 and both run clean through the new diff-family branch that forces `--no-ext-diff --no-textconv`. The PreToolUse hook refused none of my commands; every Bash call was one wrapper call with no chain, redirect or background field. Not opened this pass: `src/commands`, `commands`, `skills`, `src/skills`, `docs`, `dtd/sigil`.

### 🩺 Findings

<finding file="checker/companion-run.sh" line="150" severity="medium" confidence="measured">The comment says the ceiling number "lives here alone", but 300 is written three times in this file — line 151 for the node arm, lines 175 and 176 for the two git arms — and checker/counts-sweep.mjs:105 reads only the node arm, anchored by /ceiling\.mjs" (\d+) node "\$abs"/. Raising or lowering either git arm alone leaves SECURITY.md publishing a ceiling that is true for engines and false for git, and no control compares the three.</finding>
<finding file="checker/companion-audit.sh" line="88" severity="medium" confidence="reasoned">The comment claims "a heading the companion quotes from a file it audits never adds a fifth", but the awk on line 89 matches any line beginning with three hashes whose third field is Scope, Findings, Verdict or Next, wherever it sits — including inside a fenced quotation. artifacts/research/companion-9.0.0.md carries all four at column zero on lines 5, 15, 24 and 30, and it is the file a companion pass is likeliest to quote. Quoting one makes heads read a duplicate and LAW.COMPANION.8 refuses an otherwise sound pass. M34 hardened the finding element against exactly this quoting hazard; the heading check has no equivalent, and neither M12 nor M21 plants a quoted heading.</finding>
<finding file="checker/checker-controls.sh" line="285" severity="low" confidence="measured">r36d, the assertion that the guard is wired into the nested session at all, is two greps against the live checker/companion-audit.sh with no planted copy. M17, M17b, M17c, M23, M31 and M33 each build a mutant and print a landed proof before reading the exit code; this one alone cannot be shown to go red, so a rewrite that drops --settings would have to be caught by reading, not by the suite.</finding>
<finding file="checker/companion-run.sh" line="34" severity="low" confidence="measured">The --table arm runs sed and a while-read loop and exits at line 35, never reaching lib/ceiling.mjs, so a command the companion may run — and which M30 runs on every walk — carries no ceiling, which LAW.COMPANION.2 requires of every one. It is also a third binary behind a grant the prompt describes as node and git only. The work is a bounded read of one file, so the stall risk is nil; the declaration is what does not hold.</finding>
<finding file="checker/checker-controls.sh" line="275" severity="low" confidence="measured">The comment says "five payloads piped through the binary" and then names five blocked plus one that proceeds; hook is called six times, on lines 279, 280, 281, 282, 283 and 284. Commit 263c2a1 carries the same stale five in its subject. checker/companion-audit.dtd line 52 was corrected to six in pass 28, so the DTD and the code agree and only the control's own comment and the commit message do not.</finding>
<finding file="checker/companion-audit.sh" line="110" severity="low" confidence="measured">The comment says M30 walks seventy spellings. companion-run.sh --table emits 67 rows, and M30's own floor on line 248 is only -ge 60, so nothing in the suite contradicts the number in the comment.</finding>

### 🩺 Verdict

Nothing in this range opens a write path. The three instruments that hold LAW.COMPANION.1 and 2 are independent and each refuses by name: the allow-list is a single exact Bash grant that allow_ok proves against four planted mutants, the wrapper refuses every writer, every non-reading git verb and every argument carrying shell syntax, and the guard sees the whole command string the grant's prefix cannot. The chain, redirect, bare-command, writing-tool and background payloads are all measured blocked at exit 2 with their reasons on stderr. The diff-family change is sound and wider than M24 tests: `git log` and `git diff-tree` also take the forced `--no-ext-diff --no-textconv` and both returned output here. Laws 1 through 8 are numbered in sequence, the DTD's instrument map matches the controls it names, checker/ carries no CR and no BOM across 863 files, and SECURITY.md passes the slop gate alive with every measure inside its bound.

What is left is drift between prose and code, not a hole: three ceiling literals where the comment claims one, a heading check that is more brittle than its comment admits, one assertion without a mutant, and two stale counts. None reaches high.

### 🩺 Next

Read the ceiling from one place — have the git arms take the same variable line 151 uses, or add a control that reads all three occurrences and compares them, so counts-sweep's single regex cannot publish a number that is true for one arm only. Give the heading check the treatment M34 gave the finding element: count a heading only where no earlier line in the answer opened a fence, and plant a control that quotes `Findings` from a previous record and proves a pass still scores. Add a mutant for r36d — a copy of the runner with the `--settings` flag stripped, refused — so the guard's installation is proved rather than grepped. Correct the three stale counts: five to six at checker-controls.sh:275, seventy to sixty-seven at companion-audit.sh:110, and extend M24 to run `git log` and `git diff-tree` now that both go through the forced diff options.

COMPANION VERDICT: pass
