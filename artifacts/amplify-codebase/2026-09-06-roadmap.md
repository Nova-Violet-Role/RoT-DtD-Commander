<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

# The roadmap to 8.0.0

Release class: major; taken: no.

1. verb 14 — the gate ran on one leg five times over and nothing had run on macOS; the single-leg approach is replaced by the three-leg matrix, the ubuntu-only jobs removed
2. verb 11 — cross-OS certification lifted into cross-os.dtd, LAW.XOS.1 to 7, with lib/cross-os.mjs behind it
3. verb 12 — the seven forms of XOS.gnu are refused by file and line
4. verb 12 — lib/ceiling.mjs exits 124 where timeout is absent and checker/portable.sh chooses; both paths are tripped, because a vanishing ceiling cannot trip
5. verb 11 — the Graphic and Geometric family: geometry.dtd is the first driver in the Suite, fifty-two rungs in three band modules, and three commands whose band is #FIXED
6. verb 13 — preview.content is raised before the include, and the ask family changes shape
7. verb 11 — cc-figure.dtd, one figure and two renderers under LAW.FIG.1 to 5
8. verb 3 — the arguments sigil sat in two subset comments and the harness substituted it at every call, measured this session; the comments name the document now
9. verb 2 — the encoding sweep reads bytes in Node
10. verb 6 — the counts sweep read 22 places and the second companion pass found six stale on the release date; the sweep reads 36 places now, from 22: the control counts, the build targets, the checker span and total, the claims rows and its own size
11. verb 12 — LAW.GEOM.1 was held by a CDATA attribute and a sentence; the verb of a measure, a projection and a change is an enumeration each band raises before the driver, so a leaking band is invalid against the built DOCTYPE
12. verb 6 — the 8.0.0 changelog section was spliced with the file's own header and every instrument exited 0; release-notes refuses a section carrying the title or the SPDX comment, on every push
13. verb 8 — the cross-os sweep grown to package.json scripts and to the release job's needs line, so a tag cannot ship with a leg red
14. verb 6 — the companion scorer had no control for two verdict lines or for none; M18 and M19 trip both
15. verb 3 — lib/ceiling.mjs reported every signal as 9; the signal is mapped to its own number
16. verb 6 — the state record capped a why at 160 characters and the ledger re-emitted from it lost twenty-nine committed characters mid-word, found by the third companion pass; the cap is gone and a why of 270 round-trips whole
17. verb 6 — the checker-control count was max(M) plus one, blind to M17c, and twenty was published in three places for a script running twenty-one; the script counts what it runs and prints a total line, and the sweep reads it, the span M0 to M19 and its own size
18. verb 6 — the signal line of lib/ceiling.mjs was tripped by nothing; a child killed with SIGTERM under its ceiling exits 143 on every leg, measured on Windows as code null and signal SIGTERM, and the old flat 9 fails the control at 137
19. verb 3 — the release-needs check read every job after release and holds only the release block now, with a control that plants a needs line in a later job; a form in a package.json script is reported at the line the file declares it
20. verb 7 — the hosted packer rewrote both manifest descriptions from constants three releases old and its own C16 pinned the stale marketplace opening, found by the fourth companion pass; the descriptions are built from the tree manifests with the patterns the counts sweep holds them to, a missing pattern refuses the pack by name, and C16 compares every number the archive carries against the tree, with C16b and C16c tripping it
21. verb 3 — the control script counts once per control through ok and ko instead of pass plus fail, M17b which fired without a line is counted, the sweep refuses a total line with a failing count above zero, reads the gate step name from the script labels instead of subtracting a constant, says in its header what it measures, and runs its instruments once per leg
22. verb 6 — the packer ran its entry point at module scope, so importing one of its new exports packed a 5.4 MB archive into dist/ and exited the importer, measured by the fifth companion pass; the entry point runs only as a script, C24 imports the module in a child and proves nothing is written, and dist/ is ignored
23. verb 3 — ko is the only writer of fail on a control line and a plant mode proves one failure counts once; the gate's one invocation of the counts sweep prints every DRIFT line; the marketplace opening's three family counts are measured from the shelf and the variants on disk, read by the packer from the tree and compared by C16
