<!-- SPDX-License-Identifier: AGPL-3.0-or-later OR EUPL-1.2 -->
<!-- Copyright 2026 Saimonokuma. -->

# The possibility ledger

| id | class | verb | layer | confidence | verdict | why |
|---|---|---|---|---|---|---|
| a8f0de01 | gap | 14 | generic | measured | marked | the gate ran on one leg five times over and nothing had run on macOS; the single-leg approach is replaced by the three-leg matrix, the ubuntu-only jobs removed |
| a8f0de02 | gap | 11 | generic | measured | marked | cross-OS certification lifted into cross-os.dtd, LAW.XOS.1 to 7, with lib/cross-os.mjs behind it |
| a8f0de03 | gap | 12 | generic | measured | marked | the seven forms of XOS.gnu are refused by file and line |
| a8f0de04 | gap | 12 | generic | measured | marked | lib/ceiling.mjs exits 124 where timeout is absent and checker/portable.sh chooses; both paths are tripped, because a vanishing ceiling cannot trip |
| a8f0de08 | gap | 3 | args | measured | marked | the arguments sigil sat in two subset comments and the harness substituted it at every call, measured this session; the comments name the document now |
| a8f0de09 | gap | 2 | voice | measured | marked | the encoding sweep reads bytes in Node |
| a8f0de10 | gap | 9 | generic | measured | refused | a local Linux leg through podman needs a machine, which needs WSL2 or Hyper-V; wsl --install needs elevation and a restart, so no command runs it; recorded absent, the hosted leg carries it |
| a8f0de11 | gap | 9 | generic | measured | refused | a local macOS leg is refused by licence, XOS.macos.local; the hosted runner is the only substrate that carries it |
| b8c0de01 | gap | 6 | report | measured | marked | the counts sweep read 22 places and the second companion pass found six stale on the release date; the sweep reads 36 places now, from 22: the control counts, the build targets, the checker span and total, the claims rows and its own size |
| b8c0de02 | gap | 12 | form | measured | marked | LAW.GEOM.1 was held by a CDATA attribute and a sentence; the verb of a measure, a projection and a change is an enumeration each band raises before the driver, so a leaking band is invalid against the built DOCTYPE |
| b8c0de03 | gap | 6 | report | measured | marked | the 8.0.0 changelog section was spliced with the file's own header and every instrument exited 0; release-notes refuses a section carrying the title or the SPDX comment, on every push |
| b8c0de04 | gap | 8 | generic | measured | marked | the cross-os sweep grown to package.json scripts and to the release job's needs line, so a tag cannot ship with a leg red |
| b8c0de05 | gap | 6 | adiutor | measured | marked | the companion scorer had no control for two verdict lines or for none; M18 and M19 trip both |
| b8c0de06 | gap | 3 | generic | measured | marked | lib/ceiling.mjs reported every signal as 9; the signal is mapped to its own number |
| b8c0de07 | gap | 2 | voice | measured | refused | the sigil document was accused of steering flags: measured 0 MAJOR TRAP, 0 Discrepancy flag, 0 RECORD or adiutor, 384 lines; the two avoid lines are defensive practice inside the attack table, so nothing is rewritten |
| b8c0de08 | gap | 6 | record | measured | marked | the state record capped a why at 160 characters and the ledger re-emitted from it lost twenty-nine committed characters mid-word, found by the third companion pass; the cap is gone and a why of 270 round-trips whole |
| b8c0de09 | gap | 6 | report | measured | marked | the checker-control count was max(M) plus one, blind to M17c, and twenty was published in three places for a script running twenty-one; the script counts what it runs and prints a total line, and the sweep reads it, the span M0 to M19 and its own size |
| b8c0de10 | gap | 6 | generic | measured | marked | the signal line of lib/ceiling.mjs was tripped by nothing; a child killed with SIGTERM under its ceiling exits 143 on every leg, measured on Windows as code null and signal SIGTERM, and the old flat 9 fails the control at 137 |
| b8c0de11 | gap | 3 | generic | measured | marked | the release-needs check read every job after release and holds only the release block now, with a control that plants a needs line in a later job; a form in a package.json script is reported at the line the file declares it |
| b8c0de12 | gap | 7 | report | measured | marked | the hosted packer rewrote both manifest descriptions from constants three releases old and its own C16 pinned the stale marketplace opening, found by the fourth companion pass; the descriptions are built from the tree manifests with the patterns the counts sweep holds them to, a missing pattern refuses the pack by name, and C16 compares every number the archive carries against the tree, with C16b and C16c tripping it |
| b8c0de13 | gap | 3 | generic | measured | marked | the control script counts once per control through ok and ko instead of pass plus fail, M17b which fired without a line is counted, the sweep refuses a total line with a failing count above zero, reads the gate step name from the script labels instead of subtracting a constant, says in its header what it measures, and runs its instruments once per leg |
| b8c0de14 | gap | 6 | generic | measured | marked | the packer ran its entry point at module scope, so importing one of its new exports packed a 5.4 MB archive into dist/ and exited the importer, measured by the fifth companion pass; the entry point runs only as a script, C24 imports the module in a child and proves nothing is written, and dist/ is ignored |
| b8c0de15 | gap | 3 | generic | measured | marked | ko is the only writer of fail on a control line and a plant mode proves one failure counts once; the gate's one invocation of the counts sweep prints every DRIFT line; the marketplace opening's three family counts are measured from the shelf and the variants on disk, read by the packer from the tree and compared by C16 |
| b8c0de16 | gap | 3 | generic | measured | marked | the packer decoded its module url by hand and on a checkout path with a space its entry point never matched, a packer exiting 0 having built nothing, found by the sixth companion pass; it reads its path through fileURLToPath like the other modules, the predicate is exported and tested, and C24b runs a copy from a directory with a space |
| b8c0de17 | gap | 6 | report | measured | marked | docs/HOSTED-PLUGIN.md told a reader to run the packer controls and compare against 23 while the instrument answered 26; the doc, the changelog and the sweep carry what it prints, C16b plants stale family counts and the sweep plants a stale one in the marketplace opening |
| a8f0de05 | idea | 11 | schematic | reasoned | marked | the Graphic and Geometric family: geometry.dtd is the first driver in the Suite, fifty-two rungs in three band modules, and three commands whose band is #FIXED |
| a8f0de06 | idea | 13 | form | reasoned | marked | preview.content is raised before the include, and the ask family changes shape |
| a8f0de07 | idea | 11 | form | reasoned | marked | cc-figure.dtd, one figure and two renderers under LAW.FIG.1 to 5 |
| a8f0de12 | idea | 13 | report | reasoned | exposed | the preview figure carried out of the intake into the artifact as the first draft, which is the only honest way to lose the guessed mark; not done this run |
| a8f0de13 | idea | 9 | adiutor | reasoned | exposed | a dynamic audit for checker/*.dtd, a different instrument from the static one that was widened and reverted as c3e0de05; still unwritten |
